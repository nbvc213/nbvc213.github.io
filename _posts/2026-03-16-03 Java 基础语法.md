---
title: "03 Java 基础语法"
layout: post
date: 2026-03-16
categories: Java 笔记
math: true
mermaid: true
---

## Java 注释

注释是写给人看的说明文字，不是写给计算机执行的程序代码。在 Java 中，**注释不会被编译器当作程序逻辑处理，也不会在程序运行时执行**。因此，注释的核心作用不是“参与运算”，而是**帮助人理解代码**。

### 注释有什么用

注释的主要作用可以概括为以下几个方面：

1. **解释和说明代码。** 注释可以说明一段代码是做什么的、为什么这样写、实现思路是什么，从而提高代码的可读性。
2. **辅助调试和排错。** 在分析程序问题时，注释可以帮助开发者快速回忆某段代码的用途，也可以临时注释掉部分代码来缩小问题范围。
3. **便于后期维护。** 程序往往不是“一次写完永远不改”，而是需要不断修改和扩展。合适的注释可以降低维护成本。
4. **生成开发文档。** Java 中的文档注释可以配合 `javadoc` 命令自动生成 API 文档，方便他人查阅和使用。

### 怎么写好注释

注释并不是越多越好，而是要写得**准确、必要、易懂**。写注释时通常应注意以下几点：

1. **注释要简洁。** 能用一句话说清楚的，就不要写成一大段空话。
2. **注释要准确。** 注释必须和代码真实含义一致，不能写错，更不能“代码改了，注释没改”。
3. **注释要有意义。** 优先解释“为什么这样写”“这样设计的目的是什么”，而不是只重复代码表面意思。
4. **注释要适量。** 不是每一行代码都必须加注释，过多注释反而会影响阅读。
5. **注释要及时更新。** 当代码逻辑发生变化时，注释也应同步修改。
6. **注释要规范。** 尤其是在团队开发中，注释格式最好保持统一，便于阅读和维护。

> **易错点**：不要写这种没有实际价值的注释，例如：`i++; // i 自增 1`。这种注释只是把代码翻译成中文，没有提供额外信息，意义不大。

### Java 中的三种注释

Java 中常见的注释有三种：**单行注释**、**多行注释**、**文档注释**。

#### 单行注释

单行注释以 `//` 开头，从 `//` 开始直到本行结束的内容都会被当作注释。

```java
// 输出欢迎信息
System.out.println("欢迎学习 Java");
```

单行注释适合写简短说明，通常用于解释某一行代码或某一个小步骤。

#### 多行注释

多行注释以 `/*` 开头，以 `*/` 结尾，中间可以写多行内容。

```java
/*
这里用于说明一段较完整的逻辑：
先读取用户输入，
再进行格式校验，
最后输出处理结果。
*/
```

多行注释适合写相对完整的说明，例如某段逻辑的处理过程、注意事项等。

#### 文档注释

文档注释以 `/**` 开头，以 `*/` 结尾。它和普通多行注释长得很像，但作用不同：**文档注释主要用于生成 API 文档**。

```java
/**
 * 表示一个简单的计算器类。
 * 用于演示文档注释的基本写法。
 *
 * @author 李四
 * @version 1.0
 */
public class Calculator {
    
    /**
     * 计算两个整数的和。
     *
     * @param a 第一个整数
     * @param b 第二个整数
     * @return 两个整数的和
     */
    public static int add(int a, int b) {
        return a + b;
    }
}
```

文档注释通常写在：

- 类上
- 方法上
- 构造方法上
- 成员变量上

它可以配合 `javadoc` 命令自动生成结构化文档。

#### 文档注释中的常见标签

文档注释中可以使用一些特殊标签，用来描述更明确的信息。常见的有：

- `@author`：作者
- `@version`：版本
- `@param`：方法参数说明
- `@return`：返回值说明
- `@throws`：可能抛出的异常说明

例如：

```java
/**
 * 判断一个整数是否为偶数。
 *
 * @param num 要判断的整数
 * @return 如果是偶数返回 true，否则返回 false
 */
public static boolean isEven(int num) {
    return num % 2 == 0;
}
```

#### `javadoc` 命令

Java 提供了 `javadoc` 命令，用来根据**文档注释**自动生成 API 文档。下面先看一段示例代码：

```java
/**
 * 表示一个学生信息类。
 *
 * @author 王五
 * @version 1.0
 */
public class Student {
    
    /**
     * 输出学生的自我介绍信息。
     *
     * @param name 学生姓名
     * @param age 学生年龄
     */
    public static void introduce(String name, int age) {
        System.out.println("大家好，我叫" + name + "，今年" + age + "岁。");
    }
}
```

如果要为这段代码生成文档，可以在命令行中进入 `Student.java` 所在目录，然后执行：

```cmd
javadoc -author -version -d doc Student.java
```

这条命令中各个参数的含义如下：

- `javadoc`：表示调用 Java 的文档生成工具
- `-author`：把文档注释中的作者信息也生成到文档中
- `-version`：把文档注释中的版本信息也生成到文档中
- `-d doc`：指定输出目录为 `doc`，生成的文档会放到这个文件夹中
- `Student.java`：表示要为哪个 Java 源文件生成文档

执行完成后，当前目录下通常会生成一个名为 `doc` 的文件夹。打开该文件夹中的 `index.html`，就可以在浏览器中查看生成后的 API 文档。

> **练习**：
>
> 【单选题】关于 Java 中三种注释的说法，下列哪一项是正确的？
>
> - A. `//` 单行注释和 `/* ... */` 多行注释都可以被 `javadoc` 直接提取为 API 文档
> - B. `/** ... */` 文档注释本质上仍属于注释，但它主要用于生成 API 文档
> - C. 多行注释可以嵌套使用，只要每一层都正确写上 `/*` 和 `*/`
> - D. 注释虽然不会执行，但会被 JVM 当作程序运行时元数据加载执行
>
> 【单选题】下列关于文档注释标签的使用，哪一项是最恰当的？
>
> - A. `@param` 可以用于类的文档注释中，表示类的属性说明
> - B. `@return` 可以用于任何文档注释中，只要想说明结果即可
> - C. `@author` 和 `@version` 更常用于类的文档注释，`@param` 和 `@return` 更常用于方法的文档注释
> - D. `@throws` 只能写在 `main` 方法上，不能写在普通方法上
>
> 【单选题】下列哪一段注释最符合“有意义的注释”这一原则？
>
> - A. `i++; // i 加 1`
> - B. `sum = a + b; // 把 a 和 b 相加`
> - C. `// 这里先进行非空校验，避免后续调用方法时出现空指针异常`
> - D. `System.out.println(name); // 输出 name`
>
> 【答案】
>
> - B。`/** ... */` 是 **文档注释**，本质上仍然是注释，但它的特殊用途是配合 `javadoc` 生成 API 文档。A 错在 `javadoc` 主要处理文档注释，而不是普通单行注释和多行注释；C 错在 Java 的多行注释**不能随意嵌套**；D 错在注释不会被执行，也不会作为程序逻辑运行。
> - C。`@author`、`@version` 更常用于**类**的文档注释，`@param`、`@return`、`@throws` 更常用于**方法**的文档注释。A 错在 `@param` 一般用于方法参数，不是类属性；B 错在 `@return` 主要用于有返回值的方法；D 错在 `@throws` 可以用于任何可能抛出异常的方法说明。
> - C。C 解释了**为什么这样写**，说明了设计意图和潜在风险，因此是更有价值的注释。A、B、D 都只是把代码表面意思重复了一遍，信息增量很低。

## 标识符

在 Java 中，**标识符**就是程序员自己起的名字，用来给程序中的各种元素命名。例如变量名、方法名、类名、包名等，本质上都属于标识符。

Java 中的标识符可以用来标识很多程序元素，常见的有：**变量、方法、类、接口、枚举、注解、包、常量、类型参数** 等。

### 标识符命名规则

Java 中合法的标识符必须满足以下规则。

1. **标识符只能由字母、数字、下划线 `_` 和美元符号 `$` 组成。** 不能出现空格、横线、井号、感叹号等其他特殊符号。
2. **标识符不能以数字开头。** 也就是说，数字可以出现在中间或后面，但不能放在最前面。
3. **标识符不能是 Java 关键字。** 例如 `public`、`class`、`void`、`static` 等都不能直接拿来命名。
4. **标识符区分大小写。** 例如 `name` 和 `Name` 是两个不同的标识符。
5. **标识符长度原则上没有限制。** 但实际开发中应尽量做到简洁且有意义。

Java 对“字母”的支持范围比较广，不只限于英文字母。也就是说，Java 在语法层面支持使用很多语言中的文字作为标识符，例如中文理论上也是可以的。

> **补充**：虽然语法上允许使用中文作为标识符，但在实际开发中，通常仍然建议优先使用 **英文** 命名，这样更统一，也更利于团队协作和代码维护。

> **易错点**：单独一个下划线 `_` 在现代 Java 中**不能作为标识符单独使用**，这一点和“可以包含下划线”不是一回事。

### 标识符命名规范

命名规则解决的是“**能不能这样写**”，命名规范解决的是“**这样写好不好**”。也就是说，一个名字即使语法上合法，也不一定符合实际开发中的书写习惯。Java 中常见的命名规范如下。

| 标识内容                     | 命名规范                                                     | 示例                         |
| ---------------------------- | ------------------------------------------------------------ | ---------------------------- |
| 类名、接口名、枚举名、注解名 | **大驼峰命名法**：每个单词首字母大写                         | `StudentService`、`UserInfo` |
| 变量名、方法名               | **小驼峰命名法**：第一个单词首字母小写，后面每个单词首字母大写 | `studentName`、`doLogin`     |
| 常量名                       | **全部大写**，多个单词用下划线连接                           | `MAX_SIZE`、`LOGIN_SUCCESS`  |
| 包名                         | **全部小写**                                                 | `com.test.demo`              |

此外，命名时还应遵循一个非常重要的原则：**见名知意**。也就是说，看到名字，最好就能大致猜出它表示什么。

例如：

- `a`、`b`、`c` 这种名字通常可读性较差
- `studentName`、`orderCount`、`printMessage` 这种名字可读性更好

> **练习**：
>
> 【单选题】关于下列 4 个标识符的说法，哪一项是正确的？
>
> 1. `public1`
> 2. `first.name`
> 3. `你好`
> 4. `_`
>
> - A. 只有 1 和 3 合法
> - B. 只有 1、2、3 合法
> - C. 只有 2 和 4 合法
> - D. 1、3、4 都合法
>
> 【单选题】阅读下面代码，关于其中标识符的说法，下列哪一项是正确的？
>
> ```java
> public class UserInfo {
>     int user_age = 20;
>     int UserAge = 21;
>     int public1 = 22;
>     int $score = 23;
> }
> ```
>
> - A. 以上 4 个成员变量名都不合法
> - B. 以上 4 个成员变量名都合法，但它们并不都符合 Java 的命名规范
> - C. 只有 `public1` 合法，其他都不合法
> - D. `user_age` 不合法，因为变量名中不能出现下划线
>
> 【答案】
>
> - A。`public1` 合法，因为它不是关键字 `public` 本身；`first.name` 不合法，因为包含非法字符 `.`；`你好` 在 Java 语法层面可以作为标识符，因此合法；单独一个 `_` 在现代 Java 中不能作为标识符单独使用，因此不合法。
> - B。`user_age`、`UserAge`、`public1`、`$score` 从**语法规则**上看都属于合法标识符：它们都不是关键字本身，也没有以数字开头，也没有包含非法字符。但从**命名规范**上看，它们并不都推荐这样写：变量名通常应采用**小驼峰命名法**，因此更规范的写法通常应类似于 `userAge`；`$score` 虽然合法，但一般不建议主动使用 `$`；`UserAge` 作为变量名虽然合法，但更像类名风格。

## 关键字

在 Java 中，**关键字**是语言本身事先规定好的、具有**特殊含义**的单词。它们不是程序员随便起的名字，而是 Java 语法的一部分，用来表示特定的结构、功能或控制逻辑，例如定义类、声明类型、控制流程、处理异常、限定访问权限等。

也正因为关键字本身已经被 Java 赋予了固定含义，所以它们**不能再被当作标识符使用**。也就是说，不能拿关键字去给类、变量、方法、包等重新命名。

关键字的作用，本质上是帮助 Java 组织程序的语法和逻辑。不同类型的关键字负责不同的功能：

| 类别               | 关键字举例                                                   |
| ------------------ | ------------------------------------------------------------ |
| **基本数据类型**   | `byte`、`short`、`int`、`long`、`float`、`double`、`char`、`boolean` |
| **流程控制**       | `if`、`else`、`switch`、`case`、`default`、`for`、`while`、`do`、`break`、`continue`、`return` |
| **访问控制**       | `public`、`private`、`protected`                             |
| **类与面向对象**   | `class`、`interface`、`enum`、`extends`、`implements`、`new`、`this`、`super`、`instanceof` |
| **修饰与特性相关** | `abstract`、`final`、`static`、`native`、`strictfp`、`synchronized`、`transient`、`volatile` |
| **异常处理**       | `try`、`catch`、`finally`、`throw`、`throws`                 |
| **包与导入**       | `package`、`import`                                          |
| **其他**           | `assert`、`void`、`const`、`goto`                            |

> **注意**：`const` 和 `goto` 虽然出现在 Java 的保留字列表中，但在实际 Java 编程中**并没有真正对应的正常使用场景**。学习时先知道它们**不能拿来命名**即可。

## 字面量

在 Java 中，**字面量**就是在程序中**直接写出来的数据**。它不是变量，不需要先定义，也不需要经过额外计算，写出来就表示某个确定的值。

例如：`10`、`3.14`、`true`、`'a'`、`"Hello"`。这些都属于字面量。

Java 中常见的字面量主要有以下几类：

| 类型               | 示例                            |
| ------------------ | ------------------------------- |
| **整数型字面量**   | `10`、`-5`、`0`、`100`          |
| **浮点型字面量**   | `3.14`、`-0.5`、`1.0`           |
| **布尔型字面量**   | `true`、`false`                 |
| **字符型字面量**   | `'a'`、`'1'`、`'国'`            |
| **字符串型字面量** | `"Hello"`、`"Java"`、`"你好呀"` |

这里要特别区分 **字符型** 和 **字符串型**：

- **字符型字面量** 必须使用 **单引号** 括起来，并且单引号中**只能有一个字符**
- **字符串型字面量** 必须使用 **双引号** 括起来，双引号中可以有一个字符、多个字符，甚至可以是空字符串

例如：

- `'a'` 是字符型字面量
- `"a"` 是字符串型字面量
- `'ab'` 是错误写法
- `"ab"` 是正确的字符串型字面量

> **补充**：像 `'\t'` 这样的写法虽然看起来由两个符号组成，但它表示的是一个**转义后的单个字符**，因此它仍然是字符型字面量。

### 加号运算符 `+`

在 Java 中，**加号 `+`** 有两个作用：

1. **求和**
2. **字符串拼接**

判断规则并不复杂，核心只要记住一句话：**只要 `+` 两边有一边是字符串，`+` 就执行字符串拼接；其它情况下，`+` 执行求和。**

例如：

- `5 + 6` 是求和，结果是 `11`
- `"5" + "6"` 是字符串拼接，结果是 `"56"`
- `"5" + 6` 也是字符串拼接，结果是 `"56"`

Java 表达式在没有括号干预时，通常按照**从左到右**的顺序计算；如果有小括号，则**先算括号里面**。

例如：

- `"5" + 6 + 7` 会先得到 `"56"`，再得到 `"567"`
- `"5" + (6 + 7)` 会先算括号里的 `6 + 7`，得到 `13`，最后结果是 `"513"`

> **练习**：
>
> 【单选题】关于 Java 字面量的说法，下列哪一项是正确的？
>
> - A. `'ab'` 是合法的字符型字面量，因为它放在单引号中
> - B. `"a"` 和 `'a'` 表示的字面量类型相同
> - C. `false` 和 `"false"` 含义不同，前者是布尔型字面量，后者是字符串型字面量
> - D. `'\t'` 不是字符型字面量，因为它由两个符号组成
>
> 【简答题】阅读下面代码，程序的输出结果是？
>
> ```java
> public class LiteralTest {
>     public static void main(String[] args) {
>         System.out.println("结果1：" + 2 + 3);
>         System.out.println("结果2：" + (2 + 3));
>         System.out.println(2 + 3 + "4");
>         System.out.println(2 + (3 + "4"));
>     }
> }
> ```
>
> 【答案】
>
> - C。`false` 是 **布尔型字面量**，而 `"false"` 是 **字符串型字面量**。A 错在字符型字面量要求单引号中只能有**一个字符**，所以 `'ab'` 不合法；B 错在 `"a"` 是字符串型，`'a'` 是字符型；D 错在 `'\t'` 虽然包含转义写法，但它最终表示的是**一个字符**，所以仍然是合法的字符型字面量。
>
> - 结果是：
>
>   ```
>   结果1：23
>   结果2：5
>   54
>   234
>   ```

## 变量

在 Java 中，**变量**可以理解为一个“用来存放数据的盒子”。但更准确地说，变量本质上是**内存中的一块有名字的存储空间**，程序可以通过这个名字去访问其中保存的数据。

一个变量通常包含三个核心要素：

1. **数据类型**：决定这个变量可以存什么类型的数据，以及通常需要多大的存储空间。
2. **变量名**：也就是这块空间的名字，只要符合标识符命名规则即可。
3. **变量值**：这块空间当前实际存储的数据。

例如：

```java
int age = 20;
```

这行代码表示：

- `int` 是数据类型，表示这是一个**整数类型**的变量
- `age` 是变量名
- `20` 是变量当前保存的值

### 变量为什么叫“变量”

“变量”中的“变”，体现在：**变量中存放的值是可以改变的**。例如：

```java
int age = 20;
age = 30;
```

第一行表示变量 `age` 的初始值是 `20`，第二行表示把它的值改成 `30`。这种操作叫做：**给变量重新赋值**。也就是说，变量名一般不变，但变量中保存的数据可以变化，这就是“变量”这个名称的由来。

需要注意的是，**给变量重新赋值时，右边的值必须和左边变量的类型保持一致**，否则就会报错。例如下面这段代码就是错误的：

```java
int age = 20;
age = "30";
```

原因在于，`age` 是 `int` 类型变量，只能存放整数，而 `"30"` 是字符串，不是整数，二者类型不匹配，所以编译器会报错。

> **补充**：可以把变量理解成一个有固定类型的盒子，**什么类型的盒子，就只能装什么类型的数据**。

### 变量的作用

变量最重要的作用有两个：**提高程序的可维护性** 和 **提高程序的可读性**。

如果程序中某个数据会重复出现，直接把数字或字符串一遍遍写死在代码里，后期一旦需求变化，就要改很多地方，不仅麻烦，还容易漏改。使用变量后，只需要修改变量的值，程序中引用这个变量的地方就会自动跟着变化，因此更容易维护。

例如：

```java
double price = 58.0;
System.out.println(price * 1);
System.out.println(price * 2);
System.out.println(price * 3);
```

如果后面商品单价从 `58.0` 改成了 `66.5`，只需要改一处：

```java
double price = 66.5;
System.out.println(price * 1);
System.out.println(price * 2);
System.out.println(price * 3);
```

同时，像 `price` 这样的名字本身就带有含义，看到代码时，比直接看到一堆 `58.0` 更容易理解程序到底在算什么，这就是变量对可读性的提升。

### 变量的声明、赋值、访问

#### 变量的声明

变量在使用之前，必须先进行**声明**。所谓声明，就是告诉程序：要创建一个什么类型的变量，以及这个变量叫什么名字。

语法格式如下：`数据类型 变量名;`，其中：

- **数据类型**用于说明变量中可以存放什么类型的数据。
- **变量名**用于后续访问这个变量。

例如：

```java
int count;
String title;
double salary;
```

变量名只要符合 **标识符** 的命名规则即可。按照 Java 的常见命名规范，变量名通常采用 **小驼峰命名法**（camelCase），即**首字母小写，后面每个单词的首字母大写**，例如 `userName`、`studentAge`、`totalScore`。

#### 变量的赋值

变量声明之后，可以通过 **赋值运算符** `=` 给变量保存数据。

例如：

```java
count = 15;
title = "Java入门";
salary = 8500.5;
```

这里的 `=` 叫做**赋值运算符**，它的作用不是表示“左右相等”，而是表示：**将右边表达式计算出来的结果，赋给左边的变量**。

也就是说，赋值时的执行顺序是：

1. 先计算 `=` 右边的内容。
2. 再把结果保存到 `=` 左边的变量中。

例如：

```java
int result;
result = 8 + 4;
```

程序会先计算 `8 + 4` 的结果，得到 `12`，再把 `12` 赋值给变量 `result`。

> **注意**：变量的声明和赋值也可以写在同一行，这种写法非常常见。
>
> 例如：
>
> ```java
> int level = 3;
> ```

#### 变量的访问

变量的访问主要有两种方式：**读取**和**修改**。

**读取**就是把变量中保存的值取出来使用，例如输出变量的值：

```
System.out.println(变量名);
```

**修改**就是给已经存在的变量重新赋值，用新的值替换原来的值：

```
变量名 = 变量值;
```

### 变量的使用细节

#### 变量必须先赋值，才能访问

局部变量在**声明之后**，如果还没有完成赋值，就**不能直接访问**。也就是说，Java 不允许读取一个“还没有明确值”的局部变量。

例如：

```java
int score;
System.out.println(score); // 报错：变量 score 可能尚未初始化
```

这段代码中，`score` 虽然已经声明了，但还没有赋值，因此程序不知道它当前到底应该保存什么值，所以会直接报错。

#### 变量必须先声明，后访问

方法体中的代码遵循**自上而下、逐行执行**的顺序。程序执行到某一行时，只能使用前面已经声明过的变量，不能先访问、后声明。

例如：

```java
System.out.println(price);
int price = 50;
```

这段代码会报错，因为程序执行到 `System.out.println(price);` 这一行时，变量 `price` 还没有声明，程序根本不知道 `price` 是什么。

#### 一行可以声明多个同类型变量

在一行代码中，可以同时声明多个**相同数据类型**的变量。

例如：

```java
int x, y, z = 500;
```

这行代码表示同时声明了三个 `int` 类型变量：`x`、`y`、`z`，其中只有 `z` 被赋值为 `500`，而 `x` 和 `y` 只是声明了，并没有赋值。

#### 同一作用域中，变量名不能重复

在同一个作用域内，变量名**不能重名**。也就是说，一个名字一旦已经被某个变量使用，在同一作用域中就不能再次声明同名变量。

例如：

```java
public static void main(String[] args) {
    int count = 10;

    count = 20;
    count = 30;

    int count = 40; // 报错：变量 count 已定义
}
```

这段代码中：

- `count = 20;`
- `count = 30;`

都属于对同一个变量进行**重新赋值**，这是完全允许的。

### 变量的作用域

**作用域**，就是变量的**有效范围**。一个变量并不是在程序的任何位置都能使用，它只能在自己所属的范围内被访问。

对于初学阶段，可以先用一句话来理解：**变量出了它所属的大括号，通常就不能再访问了。**

例如：

```java
public class ScopeDemo {
    public static void main(String[] args) {
        int total = 5;

        if (total > 0) {
            int temp = 100;
            System.out.println(temp);
        }

        System.out.println(total);
        System.out.println(temp); // 报错：这里已经超出 temp 的作用域
    }
}
```

在这段代码中：

- `total` 定义在 `main` 方法中，因此它的作用域是整个 `main` 方法体。
- `temp` 定义在 `if` 语句块中，因此它只能在这个 `if` 的大括号内部使用。
- 离开 `if` 语句块后，`temp` 就失效了。

变量之所以有作用域差异，是因为它们声明的位置不同，而声明位置不同，就会导致它们的**生命周期**不同。所谓**生命周期**，就是变量从**创建**到**销毁**的整个过程，也可以理解为：从内存开辟到内存释放的过程。

#### Java 的就近原则

Java 在访问变量时，遵循**就近原则**。所谓就近原则，就是：**如果多个位置存在同名变量，优先访问离当前位置最近的那个变量**。

例如：

```java
public class NearRuleDemo {
    static int value = 10;

    public static void main(String[] args) {
        int value = 99;
        System.out.println(value);
    }
}
```

输出结果是：`99`。

原因是：

- 类中有一个成员变量 `value`
- `main` 方法中又定义了一个局部变量 `value`
- 在 `main` 方法内部访问 `value` 时，会优先访问距离更近的局部变量

这就是 Java 的**就近原则**。

### 变量的分类

Java 中的变量，按照**声明位置**的不同，可以分为两大类：

1. **局部变量**
2. **成员变量**

#### 局部变量

**局部变量**，是指声明在**方法体、语句块、形式参数**中的变量。

例如：

```java
public class VariableTypeDemo {
    public static void printSum(int a, int b) {
        int result = a + b;

        if (result > 0) {
            int flag = 1;
            System.out.println(flag);
        }

        System.out.println(result);
    }
}
```

这里的：

- `a`、`b` 是方法的**形式参数**
- `result` 是方法体中的局部变量
- `flag` 是 `if` 语句块中的局部变量

它们都属于**局部变量**。

#### 成员变量

**成员变量**，是指定义在**类中、方法外**的变量。

例如：

```java
public class Student {
    String name = "Tom";
    int age = 18;
    static String school = "JavaSchool";
}
```

这里的 `name`、`age`、`school` 都定义在类中、方法外，因此都属于**成员变量**。

> **练习**：
>
> 【单选题】关于下面这段代码的说法，哪一项是正确的？
>
> ```java
> public class Test1 {
>  public static void main(String[] args) {
>      int a, b = 10, c;
>      a = 5;
>      c = a + b;
>      System.out.println(a);
>      System.out.println(b);
>      System.out.println(c);
>  }
> }
> ```
>
> - A. 这段代码编译失败，因为 `a` 没有在声明时赋值
> - B. 这段代码编译失败，因为一行不能声明多个变量
> - C. 这段代码可以正常编译运行，并输出 `5`、`10`、`15`
> - D. 这段代码运行时报错，因为 `c` 没有初始值
>
> 【简答题】阅读下面代码，程序的输出结果是什么？请说明原因。
>
> ```java
> public class Test2 {
>     static int num = 100;
> 
>     public static void main(String[] args) {
>         int num = 50;
> 
>         if (num > 10) {
>             int result = num + 20;
>             System.out.println("A:" + result);
>         }
> 
>         System.out.println("B:" + num);
>         System.out.println("C:" + Test2.num);
>     }
> }
> ```
>
> 【综合题】阅读下面代码，回答两个问题：
>
> 1. 哪几行代码会报错？
> 2. 如果把报错的代码删除，程序最终输出什么？
>
> ```java
> public class Test3 {
>     static int x = 10;
> 
>     public static void main(String[] args) {
>         int x = 20;
>         int y;
> 
>         {
>             int z = x + 5;
>             y = z;
>             System.out.println("①" + z);
>         }
> 
>         System.out.println("②" + y);
>         System.out.println("③" + z);
>         System.out.println("④" + x);
>         System.out.println("⑤" + Test4.x);
>     }
> }
> ```
>
> 【答案】
>
> - **C**。`int a, b = 10, c;` 表示同时声明了三个 `int` 类型变量，其中只有 `b` 在声明时被赋值为 `10`，`a` 和 `c` 只是声明了。后面又执行了 `a = 5;` 和 `c = a + b;`，因此在访问它们之前都已经完成赋值，所以程序可以正常运行，输出结果依次是 `5`、`10`、`15`。
>
> - 输出结果是：
>
>   ```
>   A:70
>   B:50
>   C:100
>   ```
>
> - 第 1 问：会报错的是这行：
>
>   ```java
>   System.out.println("③" + z);
>   ```
>
>   第 2 问：删除报错代码后，程序输出结果为：
>
>   ```
>   ①25
>   ②25
>   ④20
>   ⑤10
>   ```

## 进制

### 二进制

二进制是计算机底层最基本的表示方式，只使用 `0` 和 `1` 两个数字，遵循 **满二进一** 的规则。计算机内部之所以使用二进制，是因为电子元件更容易稳定表示两种状态，例如通电与断电、高电平与低电平。

在二进制中，每一位都有自己的权值。从右向左依次为：$2^0,\ 2^1,\ 2^2,\ 2^3,\ \cdots$

例如，二进制 `1011` 中，各位的权值分别是：$2^3,\ 2^2,\ 2^1,\ 2^0$

#### 二进制与十进制的转换

**十进制转二进制**：十进制转二进制采用 **除 2 取余，逆序排列** 的方法。

例如，将十进制整数 `27` 转为二进制：

1. $27 \div 2 = 13$，余 $1$
2. $13 \div 2 = 6$，余 $1$
3. $6 \div 2 = 3$，余 $0$
4. $3 \div 2 = 1$，余 $1$
5. $1 \div 2 = 0$，余 $1$

将余数从下往上写，得到：$27_{10} = 11011_2$

**二进制转十进制**：二进制转十进制采用 **按位乘权，再求和** 的方法。

例如，将二进制 `1101` 转为十进制：

$1 \times 2^3 + 1 \times 2^2 + 0 \times 2^1 + 1 \times 2^0$

$= 8 + 4 + 0 + 1 = 13$

因此：$1101_{2} = 13_{10}$

> **补充**：所谓 **权值**，就是某一位所在位置对应的大小。在二进制中，每一位的权值都是 $2$ 的幂。

### 八进制

八进制只使用 `0` 到 `7` 这八个数字，遵循 **满八进一** 的规则。

在八进制中，每一位的权值从右向左依次为：$8^0,\ 8^1,\ 8^2,\ 8^3,\ \cdots$

#### 八进制与十进制的转换

**十进制转八进制**：十进制转八进制采用 **除 8 取余，逆序排列** 的方法。

例如，将十进制整数 `27` 转为八进制：

1. $27 \div 8 = 3$，余 $3$
2. $3 \div 8 = 0$，余 $3$

将余数从下往上写，得到：$27_{10} = 33_8$

**八进制转十进制**：八进制转十进制采用 **按位乘权，再求和** 的方法。

例如，将八进制 `346` 转为十进制：

$3 \times 8^2 + 4 \times 8^1 + 6 \times 8^0$

$= 3 \times 64 + 4 \times 8 + 6 \times 1$

$= 192 + 32 + 6 = 230$

因此：$346_8 = 230_{10}$

### 十六进制

十六进制遵循 **满十六进一** 的规则。

它使用的数字有：

- `0` 到 `9` 表示 $0$ 到 $9$
- `A`、`B`、`C`、`D`、`E`、`F` 分别表示 $10$、$11$、$12$、$13$、$14$、$15$

在十六进制中，每一位的权值从右向左依次为：$16^0,\ 16^1,\ 16^2,\ 16^3,\ \cdots$

#### 十六进制与十进制的转换

**十进制转十六进制**：十进制转十六进制采用 **除 16 取余，逆序排列** 的方法。

例如，将十进制整数 `255` 转为十六进制：

1. $255 \div 16 = 15$，余 $15$
2. 余数 $15$ 对应十六进制中的 `F`
3. $15 \div 16 = 0$，余 $15$
4. 余数 $15$ 仍对应 `F`

因此：$255_{10} = FF_{16}$

**十六进制转十进制**：十六进制转十进制采用 **按位乘权，再求和** 的方法。

例如，将十六进制 `ABCD` 转为十进制：

$A \times 16^3 + B \times 16^2 + C \times 16^1 + D \times 16^0$

$= 10 \times 16^3 + 11 \times 16^2 + 12 \times 16^1 + 13 \times 16^0$

$= 10 \times 4096 + 11 \times 256 + 12 \times 16 + 13 \times 1$

$= 40960 + 2816 + 192 + 13 = 43981$

因此：$ABCD_{16} = 43981_{10}$

#### 二进制与十六进制的转换

二进制与十六进制之间转换非常方便，因为 **4 位二进制正好对应 1 位十六进制**。

**二进制转十六进制：**转换方法是：**从右向左每 4 位分为一组，不足 4 位时，在左边补 0**，然后将每组二进制转换成对应的十六进制。

例如，将二进制 `1101011010111011` 转为十六进制：

先分组：

```
1101 0110 1011 1011
```

然后分别转换：

- `1101` 对应 `D`
- `0110` 对应 `6`
- `1011` 对应 `B`
- `1011` 对应 `B`

因此：$1101011010111011_2 = D6BB_{16}$

**十六进制转二进制：**转换方法是：**将每一位十六进制直接写成 4 位二进制**。

例如，将十六进制 `AF` 转为二进制：

- `A` 对应 `1010`
- `F` 对应 `1111`

因此：$AF_{16} = 10101111_2$

## 原码、反码、补码

### byte 与 bit

- `bit`（比特）是计算机中最小的**数据单位**，每一位只能是 `0` 或 `1`。
- `byte`（字节）是计算机中常用的**基本存储单位**，**1 个字节由 8 个比特组成**，即：$1\ \text{byte} = 8\ \text{bit}$。

常见存储单位换算如下：

- $1\text{KB} = 1024\text{byte}$
- $1\text{MB} = 1024\text{KB}$
- $1\text{GB} = 1024\text{MB}$
- $1\text{TB} = 1024\text{GB}$

> **补充**：在 Java 中，`byte` 类型占 **1 个字节**，`int` 类型占 **4 个字节**。

原码、反码、补码，都是**有符号整数在二进制中的表示方式**。对于一个二进制整数，最高位通常作为**符号位**：

- `0` 表示正数
- `1` 表示负数

计算机底层存储整数时，采用的是 **补码**。

### 正数的原码、反码、补码

对于正数来说，**原码、反码、补码完全相同**。

例如，十进制的 `+6`：

| 表示方式 | 二进制     |
| -------- | ---------- |
| 原码     | `00000110` |
| 反码     | `00000110` |
| 补码     | `00000110` |

### 负数的原码、反码、补码

对于负数来说，三者不同。

以十进制 `-6` 为例：

1. 先写出 `6` 的二进制：`00000110`
2. 得到 `-6` 的原码：符号位改为 `1`，即 `10000110`
3. 得到反码：**符号位不变，其余位取反**，即 `11111001`
4. 得到补码：**符号位不变，反码加 1**，即 `11111010`

整理如下：

| 表示方式 | 二进制     |
| -------- | ---------- |
| 原码     | `10000110` |
| 反码     | `11111001` |
| 补码     | `11111010` |

以 `byte` 类型为例，它占 $1$ 个字节，也就是 $8$ 个二进制位，因此取值范围是：$-2^7 \sim 2^7 - 1$，也就是：$-128 \sim 127$。其中：

- 最大值是 `127`，二进制补码为：`01111111`
- 最小值是 `-128`，二进制补码为：`10000000`

`-128` 是一个特殊值。在 `byte` 范围内，它的补码是：`10000000`。

> **注意**：`byte` 的范围不是 $-127 \sim 127$，而是 **$-128 \sim 127$**。也就是说，负数比正数多一个值。

### 已知负数的补码，求原码

如果一个二进制数是**负数的补码**，要反推出它的原码，可以这样做：

1. 确认它是负数，即最高位为 `1`
2. **符号位不变，其余位取反**
3. 再 **符号位不变，加 1**
4. 得到的结果就是这个负数的原码

例如，已知某负数的补码为 `10000010`，求原码：

1. 符号位不变，其余位取反，得到：`11111101`
2. 再加 `1`，得到：`11111110`

所以它的原码是：`11111110`。对应的十进制数是：$-126$。

> **练习**：
>
> 【简答题】已知一个 `byte` 类型整数的十进制值为 $-18$，请写出它的 **原码、反码、补码**。
>
> 【简答题】已知某个 `byte` 类型负数在计算机中的 **补码** 为：`10010110`。请回答下列问题：
>
> 1. 它对应的 **原码** 是什么？
> 2. 它对应的十进制数是多少？
> 3. 这个数一定是正数还是负数？为什么？
>
> 【答案】
>
> - $-18$ 的各表示如下：
>
>   | 表示方式 | 二进制     |
>   | -------- | ---------- |
>   | 原码     | `10010010` |
>   | 反码     | `11101101` |
>   | 补码     | `11101110` |
>
> - 已知补码为 `10010110`：
>
>   1. **原码**：`11101010`
>   2. **十进制数**：$-106$
>   3. **正负性**：**负数**

## 数据类型

数据类型用于规定变量能保存什么数据，也决定了数据在内存中的表示方式。学习数据类型的直接目的，是为了能够正确地定义变量。

### Java 数据类型概述

Java 的数据类型分为两大类：**基本数据类型**和**引用数据类型**。其中，基本数据类型包括 `byte`、`short`、`int`、`long`、`float`、`double`、`boolean`、`char` 这 8 种；除这 8 种之外，其他大多数类型都可以先归为**引用数据类型**，例如 `String`、数组、类、接口、枚举等。现阶段重点掌握的是基本数据类型。

基本数据类型可以分为四类：**整数类型**、**浮点类型**、**布尔类型**和**字符类型**。其中：

- 整数类型有 `byte`、`short`、`int`、`long`，用于表示整数；
- 浮点类型有 `float`、`double`，用于表示小数；
- 布尔类型是 `boolean`，只有 `true` 和 `false` 两个值；
- 字符类型是 `char`，用于表示单个字符，并且基于 Unicode 编码。

| 数据类型  | 说明                           | 占用字节数           | 取值范围                                    | 默认值     |
| --------- | ------------------------------ | -------------------- | ------------------------------------------- | ---------- |
| `byte`    | 整数，适合较小整数             | 1                    | $-2^7 \sim 2^7-1$，即 $-128 \sim 127$       | `0`        |
| `short`   | 整数，范围比 `byte` 大         | 2                    | $-2^{15} \sim 2^{15}-1$                     | `0`        |
| `int`     | 最常用的整数类型               | 4                    | $-2^{31} \sim 2^{31}-1$                     | `0`        |
| `long`    | 表示更大的整数                 | 8                    | $-2^{63} \sim 2^{63}-1$                     | `0L`       |
| `float`   | 单精度浮点数                   | 4                    | 约 $\pm 3.4028235 \times 10^{38}$           | `0.0f`     |
| `double`  | 双精度浮点数，最常用的小数类型 | 8                    | 约 $\pm 1.7976931348623157 \times 10^{308}$ | `0.0d`     |
| `boolean` | 表示真假                       | 不直接规定固定字节数 | `true / false`                              | `false`    |
| `char`    | 表示单个字符                   | 2                    | $0 \sim 65535$                              | `'\u0000'` |

**基本数据类型**保存的是具体的值本身，例如整数值、字符值、真假值；**引用数据类型**保存的不是对象本体，而是对象的引用。现阶段可以先简单记住：**8 种基本数据类型之外，其余大多数类型都可以先看成引用数据类型**，其中也包括 `String`。

> **补充**：Java 设计出 `byte`、`short`、`int`、`long` 这些整数类型，是为了让程序在不同场景下可以根据数据大小选择更合适的类型。理论上，类型选得更合适可以节省空间，但在一般学习和开发中不必过度计较，**大多数整数直接使用 `int` 即可**。如果整数大到连 `long` 都放不下，就需要使用 `BigInteger`，但它已经属于引用数据类型。

> **补充**：`float` 和 `double` 都用于表示小数，但 `double` 精度更高、使用更普遍，因此通常优先使用 `double`。如果数值计算对精度要求非常高，`float` 和 `double` 都可能不够准确，此时通常会使用 `BigDecimal`，它同样属于引用数据类型。

#### 默认值

Java 中，变量在使用前都必须先声明。对于**局部变量**来说，声明之后还必须手动赋值，否则不能直接访问；对于**成员变量**来说，如果没有手动赋值，系统会自动赋一个默认值。因此，**局部变量没有默认值，成员变量有默认值**。

基本数据类型的成员变量默认值如下：

- `byte`、`short`、`int` 的默认值都是 `0`
- `long` 的默认值是 `0L`
- `float` 的默认值是 `0.0f`
- `double` 的默认值是 `0.0d`
- `boolean` 的默认值是 `false`
- `char` 的默认值是 `'\u0000'`

其中，`'\u0000'` 表示一个空字符。

引用数据类型的成员变量默认值是 **`null`**。例如，`String` 属于引用数据类型，如果它是成员变量且没有手动赋值，那么默认值就是 `null`。

> **练习**：
>
> 【单选题】关于 Java 数据类型的说法，哪一项是正确的？
>
> - A. `String` 属于基本数据类型，因为它使用非常频繁
> - B. Java 的基本数据类型一共有 6 种
> - C. `char` 属于字符类型，`boolean` 属于布尔类型，它们都属于基本数据类型
> - D. 数组和类都属于基本数据类型
>
> 【单选题】关于变量默认值的说法，哪一项是正确的？
>
> - A. 局部变量如果没有手动赋值，系统会自动赋默认值
> - B. 成员变量如果没有手动赋值，引用数据类型默认值是 `0`
> - C. 成员变量如果没有手动赋值，`boolean` 的默认值是 `true`
> - D. 成员变量如果没有手动赋值，`char` 的默认值是 `'\u0000'`
>
> 【答案】
>
> - **C**。`char` 和 `boolean` 都属于 Java 的基本数据类型；`String`、数组、类等都属于引用数据类型；Java 的基本数据类型一共有 8 种，不是 6 种。
> - **D**。局部变量没有默认值，必须手动赋值后才能使用；成员变量有默认值，其中引用数据类型默认值是 `null`，`boolean` 的默认值是 `false`，`char` 的默认值是 `'\u0000'`。

### 整数类型

#### 整数型字面量的四种表示形式

Java 中的整数型字面量有四种常见写法：**十进制**、**二进制**、**八进制**、**十六进制**。十进制直接写数字即可；二进制以 `0b` 或 `0B` 开头；八进制以 `0` 开头；十六进制以 `0x` 或 `0X` 开头。

```java
int a = 18;      // 十进制
int b = 0b10010; // 二进制
int c = 022;     // 八进制
int d = 0x12;    // 十六进制
```

> **注意**：以 `0` 开头的整数会被当成**八进制**处理，因此这类写法要特别小心，不要和普通十进制混淆。

#### 整数型字面量默认当作 `int` 处理

Java 中，**任何整数字面量默认都按 `int` 类型处理**。如果希望某个整数字面量按 `long` 类型处理，必须在后面加 `L` 或 `l`，但通常推荐写成大写 `L`，因为小写 `l` 很容易和数字 `1` 混淆。

```java
int x = 20;
long y = 20L;
```

这一点要特别注意：判断一个字面量是否合法，首先看的是**字面量本身能不能作为 `int` 成立**，而不是先看左边变量的类型。因此，下面代码会直接编译报错：

```java
long n = 2147483648;
```

原因不是左边的 `long` 不够大，而是右边的 `2147483648` 默认先按 `int` 处理，但它已经超出了 `int` 的取值范围。正确写法应该是：

```java
long n = 2147483648L;
```

#### 自动类型转换

在基本数据类型中，**小容量类型可以直接赋值给大容量类型**，这种转换叫作**自动类型转换**。对于常见数字类型，可以先记住下面这个大小规律：

```
byte < short < int < long < float < double
```

例如：

```java
int a = 50;
long b = a;
double c = a;
```

上面这些写法都可以直接通过编译，因为都是**小容量到大容量**的转换。自动类型转换的本质是：目标类型的表示范围更大，通常能够容纳原来的值。

#### 强制类型转换

**大容量类型不能直接赋值给小容量类型**，因为这种转换**可能**会造成数据变化，因此必须由程序员显式写出强制类型转换符。也就是说，这种操作不是 Java 不会做，而是 Java 不允许你“悄悄地做”。

例如，下面代码会报错：

```java
int m = 30L;
```

因为 `30L` 是 `long` 类型字面量，而左边变量是 `int`。如果确实要转，就必须显式写出强制类型转换：

```java
int m = (int)30L;
```

强制类型转换**有可能**丢失精度；是否真的出问题，要看转换前的具体数值是否超出了目标类型的表示范围。

整数强制类型转换时，可以先粗略理解为：**保留右边低位，砍掉左边多余的高位**。

例如：

```java
byte b = (byte)130;
```

`130` 作为 `int` 的二进制补码是：

```
00000000 00000000 00000000 10000010
```

强制转成 `byte` 后，只保留最后 8 位：

```
10000010
```

这个结果在 `byte` 中是按**补码**解释的，对应的十进制值是 **`-126`**，因此最终结果为：

```java
byte b = (byte)130; // -126
```

这说明强制类型转换之后，结果不一定还是原来的数值，因为转换后的类型位数更少，可能已经发生了截断。

> **易错点**：强制类型转换后，计算机看到的不是“原来的十进制值”，而是**截断后的二进制补码**。因此结果可能和直觉不一致。

> **补充**：**整数字面量没有超出 `byte`、`short`、`char` 的取值范围，可以直接赋值**
>
> 虽然 Java 中**整数字面量默认按 `int` 处理**，但如果一个整数字面量本身没有超出目标类型的取值范围，编译器允许它直接赋值给 `byte`、`short` 或 `char` 变量。这可以看作一种**编译期的特殊处理**，目的是让代码书写更方便。
>
> ```java
> byte b = 100;
> short s = 30000;
> char c = 66;
> ```
>
> 上面三行代码都可以直接通过编译，其中 `66` 会被当作字符编码处理，因此 `c` 对应的字符是 `B`。
>
> 如果整数字面量超出了目标类型的取值范围，就不能直接赋值，必须使用强制类型转换。
>
> ```java
> byte b = (byte)130;
> ```
>
> 这行代码可以通过编译，但最终结果不是 `130`，而是 **`-126`**，因为强制类型转换后只保留低位二进制，数值已经发生变化。
>
> **注意**：这里的“可以直接赋值”只针对**整数字面量**成立，不代表所有 `int` 类型的值都能直接赋给 `byte`、`short` 或 `char`。例如下面的写法仍然会报错：
>
> ```java
> int n = 100;
> byte b = n;
> ```

> **补充**：当表达式里全部都是整数字面量时，编译器有时会在**编译阶段直接算出结果**。如果结果没有超出目标类型的范围，就允许直接赋值。这可以看作编译器做的一种常量优化。
>
> ```java
> byte m = 12 / 4;
> ```
>
> 上面这行代码可以通过编译，因为 `12 / 4` 在编译阶段就能算出结果是 `3`，而 `3` 没有超出 `byte` 的取值范围。
>
> 但如果参与运算的是变量，情况就不同了。因为变量的值在编译阶段通常不能直接确定，编译器只能根据类型判断表达式结果。例如两个 `int` 变量相除，结果类型一定还是 `int`，因此不能直接赋值给 `byte` 变量。
>
> ```java
> int p = 12;
> int q = 4;
> // byte n = p / q; // 编译报错
> ```
>
> 如果确实要赋值给 `byte`，要么把接收结果的变量改成 `int`，要么显式进行强制类型转换。
>
> ```java
> int p = 12;
> int q = 4;
> byte n = (byte)(p / q);
> ```
>
> 这里的小括号不能省略。因为强制类型转换如果只作用在某一个操作数上，并不会改变整个表达式最终的结果类型。例如：
>
> ```java
> int p = 12;
> int q = 4;
> // byte n = (byte)p / q; // 仍然报错
> ```
>
> 这段代码中，只是把 `p` 转成了 `byte`，但 `byte` 和 `int` 一起运算时，仍然会先提升为 `int`，所以整个表达式结果还是 `int`，仍然不能直接赋值给 `byte`。

#### 整数运算中的类型提升

在 Java 中，两个 `int` 类型的数据做运算时，结果仍然是 `int` 类型。因此，如果两个整数相除，结果只会保留整数部分，不会自动变成小数。

```java
int a = 14;
int b = 4;
int c = a / b;
System.out.println(c); // 3
```

如果多种数字类型混合运算，Java 会先把它们统一提升到**参与运算的最大类型**，再进行计算。例如 `byte`、`int`、`long` 一起运算时，会先提升到 `long`，因此最终结果也是 `long` 类型。

```java
byte x = 20;
int y = 50;
long z = 80L;
long result = x + y + z;
```

如果把接收结果的变量 `result` 写成 `int`，就无法接收一个已经提升为 `long` 的运算结果。

> **练习**：
>
> 【单选题】阅读下面代码，关于程序的编译与运行结果，哪一项说法是正确的？
>
> ```java
> public class Test1 {
>     public static void main(String[] args) {
>         int a = 010;
>         int b = 0x10;
>         int c = 0b10;
>         long d = 2147483648L;
>         int e = (int)d;
>         System.out.println(a);
>         System.out.println(b);
>         System.out.println(c);
>         System.out.println(e);
>     }
> }
> ```
>
> - A. 程序编译失败，因为 `010` 和 `0b10` 不能作为整数型字面量
> - B. 程序可以正常编译运行，输出依次为 `10`、`16`、`2`、`2147483648`
> - C. 程序可以正常编译运行，输出依次为 `8`、`16`、`2`、`-2147483648`
> - D. 程序编译失败，因为 `2147483648L` 超出了 `int` 的取值范围
>
> 【改错题】下面代码中有 **3 处编译错误**。请指出是哪 3 行，并将整段代码改成可以正常编译运行的版本。
>
> ```java
> public class Test2 {
>     public static void main(String[] args) {
>         byte a = 128;
>         short b = 200;
>         byte c = 10 + 20;
>         byte d = b;
>         long e = 3000000000;
>         int f = (int)20L;
>         System.out.println(c);
>         System.out.println(f);
>     }
> }
> ```
>
> 【简答题】阅读下面代码，程序的输出结果是什么？请说明原因。
>
> ```java
> public class Test3 {
>     public static void main(String[] args) {
>         byte a = 40;
>         byte b = 50;
>         int c = a + b;
>         byte d = (byte)(a + b + 100);
>         long e = c + d;
>         System.out.println(c);
>         System.out.println(d);
>         System.out.println(e);
>     }
> }
> ```
>
> 【答案】
>
> - **C**。`010` 是八进制，对应十进制 `8`；`0x10` 是十六进制，对应十进制 `16`；`0b10` 是二进制，对应十进制 `2`；`2147483648L` 是合法的 `long` 字面量，但强制转换为 `int` 后会发生截断，结果为 `-2147483648`。
> 
>- 第 2 题中报错的是这 3 行：
> 
>   ```java
>   byte a = 128;
>   byte d = b;
>   long e = 3000000000;
>   ```
> 
>   一种可行的修改方式如下：
> 
>   ```java
>   public class Test2 {
>       public static void main(String[] args) {
>           byte a = (byte)128;
>           short b = 200;
>           byte c = 10 + 20;
>           byte d = (byte)b;
>           long e = 3000000000L;
>           int f = (int)20L;
>           System.out.println(c);
>           System.out.println(f);
>       }
>   }
>   ```
>
> - 第 3 题输出结果是：
>
>   ```java
>    90
>   -66
>    24
>   ```

### 浮点数类型

浮点型用于表示**带小数点的数值**。Java 中的浮点型有两种：`float` 是**单精度**，占 `4` 个字节；`double` 是**双精度**，占 `8` 个字节。`double` 的表示范围和精度都更高，因此实际开发中通常更常用 `double`。

同一个小数分别用 `float` 和 `double` 保存时，`double` 往往能保留更多有效位。

```java
double d = 1.234567890123;
float f = 1.234567890123F;
System.out.println(d);
System.out.println(f);
```

运行时通常可以看到，`float` 输出的小数位会更少，这说明它的精度低于 `double`。

#### 浮点型字面量默认被当作 `double`

Java 中，浮点型字面量默认按 **`double`** 处理。如果希望一个浮点型字面量按 **`float`** 处理，必须在后面加 `F` 或 `f`，通常推荐写大写 `F`。

```java
float a = 2.5F;
double b = 2.5;
```

如果直接写：

```java
float a = 2.5;
```

就会编译报错，因为右边的 `2.5` 默认是 `double`，而 `double` 不能直接赋值给 `float`。

#### 浮点型数据的两种表示形式

浮点型字面量有两种常见写法：**十进制写法**和**科学计数法写法**。

- 十进制写法就是普通的小数形式，例如 `3.14`、`0.5`、`.5`；
- 科学计数法则使用 `E` 或 `e` 表示乘以 `10` 的多少次方，例如 `6.2E3` 表示 $6.2 \times 10^3$，`4.8E-2` 表示 $4.8 \times 10^{-2}$。

```java
double x = 4.75;
double y = .75;
double z = 6.2E3;
double k = 4.8E-2;
```

#### 浮点型数据的存储原理

浮点数在计算机底层不是按普通十进制小数的形式直接存储的，而是按一种类似“科学计数法”的方式存储。以 `float` 为例，它占 `4` 个字节，也就是 `32` 位，通常由 **符号位**、**指数位** 和 **尾数位** 三部分组成。

- **符号位**：用于表示正负，`0` 表示正数，`1` 表示负数。
- **指数位**：用于表示数量级，可以理解为科学计数法中的指数部分。例如 `0.123E30` 中的 `30`，就对应数量级的变化。也正因为有指数位，浮点数可以表示非常大或非常小的数。
- **尾数位**：用于表示有效数字，也就是数值本身的精度部分。

> **补充**：正是因为浮点数采用了“符号位 + 指数位 + 尾数位”这种存储方式，其中**指数位决定了数值可以按指数级放大或缩小**，所以 `float` 虽然只有 `4` 个字节，但它能表示的数值范围却可以超过 `long`。这说明：**类型占用的字节数更多，并不一定代表它的数值范围一定更大**。

浮点数在计算机底层通常采用**近似值**存储，而不是精确值存储。这是因为计算机的二进制位数是有限的，但现实中很多小数在二进制中可能是无限循环的，无法被有限位数完整表示，所以只能保存一个足够接近的近似值。一般来说，参与存储有效数字的位数越多，精度就越高，这也是为什么 `double` 的精度通常高于 `float`。

正因为浮点数常常是近似存储，所以**浮点数参与运算后的结果，通常也只是近似值**，不能想当然地认为它一定和我们看到的十进制结果完全一致。例如：

```java
double a = 0.3;
double b = 0.2;
double c = a - b;
System.out.println(c);
```

这段代码输出的通常不是精确的 `0.1`，而是一个非常接近 `0.1` 的值。

因此，只要是**浮点数运算得到的结果**，一般都**不建议直接使用 `==` 做相等比较**。例如下面这种写法就可能得不到预期结果：

```java
double a = 0.3;
double b = 0.2;
double c = a - b;

if (c == 0.1) {
    System.out.println("相等");
}
```

更合理的做法是比较两个浮点数的**差值是否足够小**。也就是说，不要求它们“绝对相等”，而是只要它们之间的误差小到可以接受，就认为它们相等。

```java
double a = 0.3;
double b = 0.2;
double c = a - b;

if (Math.abs(c - 0.1) < 0.000001) {
    System.out.println("相等");
}
```

> **练习**：
>
> 【单选题】阅读下面代码，关于程序的编译与运行结果，哪一项说法是正确的？
>
> ```java
> public class Test1 {
>     public static void main(String[] args) {
>         double a = 3.5;
>         float b = 3.5F;
>         double c = .5;
>         double d = 5E-1;
>         System.out.println(a == b);
>         System.out.println(c == d);
>     }
> }
> ```
>
> - A. 程序编译失败，因为 `double a = 3.5;` 中的 `3.5` 不是合法字面量
> - B. 程序编译失败，因为 `float b = 3.5F;` 中 `F` 不能出现在浮点数字面量后
> - C. 程序可以正常编译运行，输出依次为 `true`、`true`
> - D. 程序可以正常编译运行，输出依次为 `false`、`true`
>
> 【简答题】阅读下面代码，程序的输出结果是什么？请说明原因。
>
> ```java
> public class Test2 {
>     public static void main(String[] args) {
>         double x = 6.9;
>         double y = 3.0;
>         double z = x / y;
> 
>         System.out.println(z);
>         System.out.println(z == 2.3);
>         System.out.println(Math.abs(z - 2.3) < 0.000001);
>     }
> }
> ```
>
> 【简答题】阅读下面代码，程序哪些语句会报错？删除报错语句后，程序最终输出什么？
>
> ```java
> public class Test3 {
>     public static void main(String[] args) {
>         double a = 2.5;
>         float b = 2.5F;
>         float c = 2.5;
>         double d = 2.5F;
>         double e = 1.2E3;
>         float f = (float)1.2E3;
>         double g = b + e;
>         float h = b + 1.0F;
>         float i = b + 1.0;
> 
>         System.out.println(d);
>         System.out.println(e);
>         System.out.println(f);
>         System.out.println(g);
>         System.out.println(h);
>     }
> }
> ```
>
> 【答案】
>
> - **C**。`3.5` 默认是 `double`，`3.5F` 是 `float`，都合法；`.5` 和 `5E-1` 都表示十进制的 `0.5`，所以第二个比较结果是 `true`。本题中 `a` 和 `b` 表示的数值相同，比较结果也是 `true`。
>
> - 输出结果是：
>
>   ```java
>   2.3000000000000003
>   false
>   true
>   ```
>
>   原因是浮点数在底层通常按近似值存储，`6.9 / 3.0` 的结果并不是精确的 `2.3`，因此不能直接用 `==` 判断是否相等；但它与 `2.3` 的误差很小，所以用“差值是否足够小”的方式判断时结果为 `true`。
>
> - 报错语句是：
>
>   ```java
>   float c = 2.5;
>   float i = b + 1.0;
>   ```
>
>   删除报错语句后，程序输出结果为：
>
>   ```java
>   2.5
>   1200.0
>   1200.0
>   1202.5
>   3.5
>   ```

### 字符类型

`char` 表示**字符类型**，占 `2` 个字节，取值范围是 $0 \sim 65535$。它和 `short` 占用的字节数相同，但两者含义不同：`short` 用于表示整数，`char` 用于表示字符；同时，`char` 没有负数，因此可以表示更大的非负整数范围。

`char` 类型的字面量必须使用**单引号**括起来，并且其中只能写**一个字符**，例如 `'B'`、`'7'`、`'中'`、`'#'` 都是合法的字符型字面量。

```java
char c1 = 'B';
char c2 = '7';
char c3 = '中';
char c4 = '#';
```

`char` 中的内容必须是单个字符，因此不能写多个字符。例如 `'xy'` 这样的写法是错误的，因为它已经不是一个字符，而是两个字符。

```java
// char c = 'xy'; // 编译报错
```

Java 中的 `char` 可以存储一个汉字，这是因为 `char` 底层基于 **Unicode 编码**。也就是说，`char` 存储的本质上不是“字形”，而是这个字符对应的编码值。

空单引号也是不合法的，因为 `''` 里面没有任何字符，而 `char` 变量必须接收一个字符，所以这种写法会编译报错。

```java
// char c = ''; // 编译报错
```

如果确实需要表示一个**空字符**，可以使用 `'\u0000'`。这里的 `\u` 表示后面跟的是一个 Unicode 编码值，`0000` 对应的就是空字符。

```java
char c = '\u0000';
```

> **注意**：**空字符**和**空格字符**不是一回事。`'\u0000'` 表示空字符，而 `' '` 表示空格，它们是两个完全不同的字符。

#### 转义字符

转义字符是以反斜线 `\` 开头的一类特殊字符序列，用来表示那些**不能直接写出**、**写出来容易产生歧义**，或者**本身就有特殊作用**的字符。常见转义字符如下：

| 转义字符 | 含义                 |
| -------- | -------------------- |
| `\t`     | 制表符，相当于 `Tab` |
| `\n`     | 换行符               |
| `\r`     | 回车符               |
| `\"`     | 双引号               |
| `\'`     | 单引号               |
| `\\`     | 反斜线本身           |

这些转义字符既可以出现在字符串中，也可以出现在字符中。例如，要在字符串里输出双引号，就不能直接写 `"`，而要写成 `\"`；要表示一个换行，就可以使用 `\n`。

```java
System.out.println("Hello\tJava");
System.out.println("第一行\n第二行");
System.out.println("\"Java\"");
System.out.println('\\');
```

> **注意**：转义字符虽然由两个字符组成，例如 `\n`，但它在程序中表示的是**一个特殊字符**。

#### 字符编码

计算机底层只能存储二进制，因此字符在存入计算机之前，必须先按照某种规则转换成二进制；而二进制在显示给人看之前，也必须再按照对应规则还原成字符。这套“字符和二进制之间的对应规则”，就叫做**字符编码**。

其中，把字符转换成二进制的过程叫做**编码**，把二进制还原成字符的过程叫做**解码**。例如，字符 `'A'` 按某种编码规则转换成一段二进制，这叫编码；把这段二进制再按同一规则还原成 `'A'`，这叫解码。

如果编码和解码使用的不是同一套字符集或同一套规则，就可能出现**乱码**。因此，避免乱码的关键就是：**编码时用什么规则，解码时也要用同样的规则**。

##### 常见的字符编码

常见字符编码很多，初学阶段重点了解几个最常见的即可：

- **ASCII**：主要用于英文、数字和基础符号，表示范围较小。
- **ISO-8859-1（Latin-1）**：也是单字节编码，能表示更多西文字符，并且向上兼容 ASCII。
- **Unicode**：一种统一的字符编号标准，目标是表示世界上各种语言中的字符。
- **UTF-8**：基于 Unicode 的可变长编码方式，英文通常占 `1` 个字节，汉字通常占 `3` 个字节，是目前最常见的编码方式之一。
- **GBK**：常见的中文编码方式，主要用于简体中文环境。
- **Big5**：常见的繁体中文编码方式。

##### ASCII 码

ASCII 是早期非常常见的一种字符编码，主要用于表示英文字母、数字、常见符号和控制字符。学习阶段不必整张表都背下来，但下面几个值建议记住：

- `'A'` 对应 `65`，因此 `'B'` 对应 `66`
- `'a'` 对应 `97`，因此 `'b'` 对应 `98`
- `'0'` 对应 `48`，因此 `'1'` 对应 `49`

这也是为什么有些字符既能按字符看，也能按数值理解。比如 `'A'`、`'a'`、`'0'` 在底层本质上都对应一个编码值。

##### Unicode

在 Java 中，`char` 和字符串都和 **Unicode** 密切相关。学习阶段可以先记住：Java 程序里可以直接使用 Unicode 转义形式来表示一个字符，写法是 `\u` 后面跟 4 位十六进制数。

```java
char c = '\u0042';
System.out.println(c);
```

上面代码输出的是：`B`。

因为 `\u0042` 对应的就是字符 `B`。这也说明，字符在程序里不仅可以直接写成 `'B'`，也可以写成对应的 Unicode 形式。

#### `char` 运算

`char` 本质上对应一个整数编码值，因此它既可以保存字符，也可以在一定范围内接收整数字面量。**只要整数字面量没有超出 `char` 的取值范围 $0 \sim 65535$，就可以直接赋值给 `char` 变量**；但变量最终保存的是字符，因此输出时看到的是对应字符，而不是那个整数本身。

```java
char c = 97;
System.out.println(c); // a
```

如果赋的整数超出了 `char` 的取值范围，就会编译报错。

```java
char c = 65536; // 编译报错
```

**`char` 参与算术运算时，会先按对应的编码值参与计算**。如果和 `int` 一起运算，那么 `char` 会先提升为 `int`，运算结果也会是 `int`。

```java
System.out.println('a' + 1); // 98
```

这是因为 `'a'` 对应的编码值是 `97`，所以表达式实际计算的是 `97 + 1`，结果自然是 `98`。

**如果一个表达式的最终结果仍然在 `char` 的取值范围内，并且编译器能在编译阶段直接算出结果，那么这个结果也可以直接赋值给 `char` 变量。**

```java
char c = 'a' + 1;
System.out.println(c); // b
```

这里编译器会先算出 `'a' + 1` 的结果是 `98`，再把 `98` 当作字符编码赋值给 `c`，因此输出的是 `b`。

**当 `byte`、`short`、`char` 混合运算时，它们都会先提升为 `int`，再进行计算**，因此表达式结果是 `int`，不能直接赋值给 `short` 或 `char` 这类更小的类型变量。

```java
byte b = 1;
short s = 1;
char c = 1;
// short num = b + s + c; // 编译报错
```

这段代码报错的根本原因，不是结果数值太大，而是 `b + s + c` 的结果类型已经是 `int`，而 `int` 不能直接赋值给 `short`。

可以，改成下面这样更统一。

> **练习**：
>
> 【单选题】关于 Java 中 `char` 类型与字符字面量的说法，下列哪一项是正确的？
>
> - A. `char c = '';` 合法，因为空单引号表示空字符
> - B. `char c = 'ab';` 合法，因为 `'ab'` 会被当作两个字符组成的字符常量
> - C. `char c = '\n';` 合法，因为 `\n` 虽然由两个符号组成，但表示一个换行字符
> - D. `char c = "中";` 合法，因为 `char` 可以保存一个汉字
>
> 【简答题】阅读下面代码，写出程序的输出结果，并说明原因。
>
> ```java
> public class CharTest1 {
>     public static void main(String[] args) {
>         char c1 = 97;
>         char c2 = '\u0042';
>         System.out.println(c1);
>         System.out.println(c2);
>         System.out.println(c1 + 1);
>         System.out.println((char)(c2 + 1));
>     }
> }
> ```
>
> 【单选题】阅读下面代码，关于第 5 行的说法，下列哪一项是正确的？
>
> ```java
> public class CharTest2 {
>     public static void main(String[] args) {
>         byte b = 1;
>         short s = 2;
>         char c = 3;
>         short x = b + s + c;
>     }
> }
> ```
>
> - A. 可以编译通过，因为 `1 + 2 + 3 = 6` 没有超出 `short` 的范围
> - B. 可以编译通过，因为 `byte`、`short`、`char` 都属于小范围整数类型
> - C. 编译报错，因为 `b + s + c` 的结果类型会提升为 `long`
> - D. 编译报错，因为 `b + s + c` 的结果类型会提升为 `int`，不能直接赋值给 `short`
>
> 【简答题】下面有两部分问题，请分别回答。
>
> 1. 什么是**编码**和**解码**？
> 2. 为什么同样一段文本，在某些情况下会出现**乱码**？请结合字符编码的角度说明原因。
>
> 【答案】
>
> - C。`\n` 是**转义字符**，虽然写法上由两个符号组成，但在 Java 中表示的是**一个字符**，因此可以赋值给 `char`。A 错在 `''` 中没有任何字符，不能作为字符型字面量；B 错在 `char` 字面量中只能有**一个字符**，`'ab'` 包含两个字符；D 错在 `"中"` 是**字符串字面量**，不是字符字面量，`char` 必须使用**单引号**。
>
> - 输出结果是：
>
>   ```
>   a
>   B
>   98
>   C
>   ```
>
>   原因如下：
>
>   1. `char c1 = 97;` 中，`97` 没有超出 `char` 的取值范围，因此可以直接赋值；`97` 对应的字符是 `a`，所以第一行输出 `a`。
>   
>    2. `'\u0042'` 是 Unicode 写法，`\u0042` 对应字符 `B`，所以第二行输出 `B`。
>    3. `c1 + 1` 中，`char` 参与算术运算时会先提升为 `int`，因此实际计算的是 `97 + 1`，结果为 `98`，所以第三行输出 `98`。
>    4. `c2 + 1` 的结果也是 `int`，`'B'` 的编码值是 `66`，加 `1` 后得到 `67`；再通过强制类型转换 `(char)` 转回字符，因此第四行输出 `C`。
>   
> - D。`byte`、`short`、`char` 在参与算术运算时，都会先提升为 **`int`**，因此表达式 `b + s + c` 的结果类型是 `int`，不能直接赋值给 `short`。A 和 B 错在它们只看到了“数值范围”，没有注意到**表达式结果类型提升**的问题；C 错在这里提升到的是 `int`，不是 `long`。
>
> - **编码**，是把字符按照某种字符编码规则转换成二进制数据的过程；**解码**，是把二进制数据按照某种字符编码规则还原成字符的过程。出现乱码的根本原因，通常是**编码和解码所使用的规则不一致**。例如，文本在保存时如果使用的是一种编码规则，而读取显示时却使用了另一种编码规则，那么同一段二进制数据就可能被错误地解释成别的字符，最终显示为乱码。因此，避免乱码的关键是：**编码时使用什么规则，解码时也要使用同样的规则**。这也是字符编码问题中的核心原则。
>

### 布尔类型

#### 关于布尔型的值

Java 中的 **布尔型** 关键字是 `boolean`，用于表示逻辑上的“真”与“假”。`boolean` 类型只有两个取值：`true` 和 `false`，分别表示真和假。Java 中的布尔值是一个独立的数据类型，**不能用 `1` 和 `0` 替代**，因此像 `boolean gender = 1;` 这样的写法是错误的。

布尔型的设计目的，是让程序直接描述“条件是否成立”“状态是否开启”“结果是否满足”等逻辑问题。正因为它只有真和假两个值，所以特别适合参与条件判断和流程控制。

> **注意**：`true` 和 `false` 是布尔字面值，不是字符串，也不是数字。

#### 布尔值通常使用在哪

`boolean` 主要用于表示各种逻辑状态，在程序中常见的使用场景包括条件判断、逻辑运算、方法返回结果以及开关标记等。它的核心作用不是“存数据”，而是“控制程序如何执行”。

- **条件判断**：在 `if`、`while` 等语句中，条件表达式最终都必须得到一个布尔值。程序会根据结果是 `true` 还是 `false`，决定执行哪一部分代码。
- **逻辑运算**：布尔值是逻辑运算的基础，常与逻辑与 `&&`、逻辑或 `||`、逻辑非 `!` 等运算符一起使用，用来组合多个条件或对条件结果取反。
- **方法返回值**：某些方法执行后，只需要告诉调用者“是否成功”“是否满足条件”“是否存在”，这时就很适合返回 `boolean` 类型。
- **开关标记**：程序中经常需要记录某个状态是否开启、某个功能是否启用、某个条件是否已经完成，这类“是 / 否”性质的信息通常都用布尔变量表示。

> **结论**：凡是只需要表达“成立”或“不成立”、“开启”或“关闭”、“有”或“没有”这类二选一状态的地方，通常都适合使用 `boolean`。

> **练习**：
>
> 【单选题】关于 Java 中 `boolean` 类型的说法，下列哪一项是正确的？
>
> - A. `boolean` 类型可以直接保存 `0` 和 `1`
> - B. `boolean` 类型只有 `true` 和 `false` 两个值
> - C. `boolean` 本质上是整数类型的一种
> - D. 在 Java 中，`"true"` 和 `true` 完全等价
>
> 【单选题】阅读下面代码，程序的输出结果是？
>
> ```
> public class BooleanTest {
>     public static void main(String[] args) {
>         boolean flag = false;
>         if (flag) {
>             System.out.println("A");
>         } else {
>             System.out.println("B");
>         }
>     }
> }
> ```
>
> - A. 输出 `A`
> - B. 输出 `B`
> - C. 输出 `true`
> - D. 编译报错
>
> 【答案】
>
> - B。Java 中 **`boolean` 类型只有 `true` 和 `false` 两个值**，不能使用 `0` 和 `1` 代替。A 错在 Java 不支持用数字直接表示布尔值；C 错在 `boolean` 是独立的基本数据类型，不属于整数类型；D 错在 `"true"` 是**字符串**，`true` 是**布尔值**，两者类型和含义都不同。
> - B。`flag` 的值是 `false`，因此 `if (flag)` 的条件不成立，程序会执行 `else` 分支，输出 `B`。A 错在只有当条件为 `true` 时才会进入 `if` 分支；C 错在程序输出的是字符串 `"B"`，不是布尔值；D 错在这段代码语法完全正确，不会编译报错。

## 运算符

运算符（Operator）用于对数据进行运算、比较、判断、赋值或创建对象。Java 中的运算符种类较多，但在学习初期，重点应放在 **算术运算符、关系运算符、逻辑运算符、赋值运算符和条件运算符** 上；其余运算符先建立整体认识，后续再分别深入。

### 运算符总览

Java 中常见运算符可按功能分为下表所示几类：

| **运算符类型** | **运算符**                                                   | **说明**                                 |
| -------------- | ------------------------------------------------------------ | ---------------------------------------- |
| 算术运算符     | `+`、`-`、`*`、`/`、`%`、`++`、`--`                          | 用于数值运算、自增、自减                 |
| 关系运算符     | `==`、`!=`、`>`、`<`、`>=`、`<=`                             | 用于比较两个值，结果是布尔值             |
| 逻辑运算符     | `&`、`|`、`!`、`&&`、`||`                                    | 用于逻辑判断与条件组合                   |
| 按位运算符     | `&`、`|`、`^`、`~`、`<<`、`>>`、`>>>`                        | 按二进制位进行运算                       |
| 赋值运算符     | `=`、`+=`、`-=`、`*=`、`/=`、`%=`、`&=`、`|=`、`^=`、`<<=`、`>>=`、`>>>=` | 用于赋值或在赋值时附带运算               |
| 条件运算符     | `? :`                                                        | 三目运算符，根据条件返回两个结果中的一个 |
| 类型判断运算符 | `instanceof`                                                 | 判断对象是否属于某种类型                 |
| 对象创建运算符 | `new`                                                        | 创建对象或数组，分配内存                 |
| 成员访问运算符 | `.`                                                          | 访问对象的属性或方法                     |

其中，**关系运算符的运算结果一定是 `boolean` 类型**，而逻辑运算符通常就是对这些布尔结果继续进行组合判断。

> **补充**：有些符号在不同场景下含义不同，例如 `&` 和 `|` 既可以作为**逻辑运算符**，也可以作为**按位运算符**，具体要看操作数的类型和表达式的语境。

### 运算符优先级

当一个表达式中同时出现多个运算符时，Java 会按照既定的优先级规则进行计算。**优先级高的先执行，优先级低的后执行**；如果多个运算符处于同一优先级，再结合结合性决定运算顺序。

下表给出常见运算符的优先级顺序，按 **从高到低** 排列：

| **运算符类型** | **运算符**                                                   |
| -------------- | ------------------------------------------------------------ |
| 后缀运算符     | `++`、`--`                                                   |
| 一元运算符     | `+`、`-`、`~`、`!`                                           |
| 乘性运算符     | `*`、`/`、`%`                                                |
| 加性运算符     | `+`、`-`                                                     |
| 移位运算符     | `<<`、`>>`、`>>>`                                            |
| 关系运算符     | `<`、`<=`、`>`、`>=`、`instanceof`                           |
| 相等运算符     | `==`、`!=`                                                   |
| 按位与运算符   | `&`                                                          |
| 按位异或运算符 | `^`                                                          |
| 按位或运算符   | `|`                                                          |
| 逻辑与运算符   | `&&`                                                         |
| 逻辑或运算符   | `||`                                                         |
| 条件运算符     | `? :`                                                        |
| 赋值运算符     | `=`、`+=`、`-=`、`*=`、`/=`、`%=`、`&=`、`|=`、`^=`、`<<=`、`>>=`、`>>>=` |

从整体上看，可以先记住这样一条基本规律：**单目优先于双目，算术优先于比较，比较优先于逻辑，逻辑优先于赋值**。这条规律虽然不能覆盖所有细节，但足以帮助初学阶段理解大多数表达式的执行顺序。

在表达式中，**圆括号 `()`** 可以**明确改变运算顺序**，也可以让表达式的含义更清楚。即使某些表达式在不加括号时也能按照默认优先级正确执行，适当使用括号仍然是良好的编程习惯，因为它能降低阅读和理解的难度。

当一个表达式涉及多种运算符，尤其同时包含算术、比较和逻辑运算时，单纯依赖优先级往往容易出错；这时应优先通过括号把运算层次写清楚，而不是让读代码的人去“猜”默认顺序。

> **注意**：不要试图一开始就死记所有优先级细节。现阶段更重要的是掌握常见顺序，并养成在复杂表达式中主动加括号的习惯。

> **练习**：
>
> 【单选题】关于 Java 运算符及其优先级的说法，下列哪一项是正确的？
>
> - A. 在 Java 中，`&&` 的优先级高于 `==`，因此比较运算通常最后执行
> - B. `&` 和 `|` 只能作为按位运算符使用，不能参与逻辑判断
> - C. 关系运算符的结果是 `boolean` 类型，因此它们常作为逻辑运算符的操作数
> - D. 赋值运算符的优先级高于三目运算符 `? :`，因此赋值通常先执行
>
> 【简答题】阅读下面代码，写出程序的输出结果，并说明每一步的运算顺序。
>
> ```java
> public class OperatorTest {
>     public static void main(String[] args) {
>         int a = 2;
>         int b = 3;
>         int c = 4;
>         boolean result = a + b * c > 10 && a == 2 || false;
>         System.out.println(result);
>         System.out.println(a + b > c ? a + "-" + b : a * b + c + "");
>     }
> }
> ```
>
> 【答案】
>
> - C。`<`、`>`、`<=`、`>=`、`==`、`!=` 这类运算符的结果都是 **`boolean`**，因此它们常作为 `&&`、`||`、`!` 等逻辑运算符的操作数。A 错在 **比较运算符和相等运算符的优先级高于逻辑运算符**，不是 `&&` 更高；B 错在 `&` 和 `|` 既可以用于**按位运算**，也可以用于**逻辑运算**，具体要看操作数类型和语境；D 错在 **赋值运算符优先级低于三目运算符**，赋值通常更晚执行。
>
> - 输出结果是：
>
>   ```
>   true
>   2-3
>   ```
>
>   - 第一行 `boolean result = a + b * c > 10 && a == 2 || false;` 的运算顺序如下：
>
>     1. 先算乘法：`b * c`，即 `3 * 4 = 12`
>     2. 再算加法：`a + 12`，即 `2 + 12 = 14`
>     3. 再算关系运算：`14 > 10`，结果是 `true`
>     4. 再算相等运算：`a == 2`，结果是 `true`
>     5. 再算逻辑与：`true && true`，结果是 `true`
>     6. 最后算逻辑或：`true || false`，结果仍然是 `true`
>
>     因此第一行输出 `true`。
>
>   - 第二行 `System.out.println(a + b > c ? a + "-" + b : a * b + c + "");` 的运算顺序如下：
>
>     1. 先算条件部分中的加法：`a + b = 2 + 3 = 5`
>     2. 再进行比较：`5 > 4`，结果是 `true`
>     3. 因为三目运算符条件为真，所以只执行前半部分：`a + "-" + b`
>     4. 这里从左到右进行字符串拼接：`2 + "-"` 得到 `"2-"`，再和 `3` 拼接，得到 `"2-3"`
>
>     因此第二行输出 `2-3`。

### 算术运算符

**算术运算符**用于完成最基本的数值计算，是最常用的一类运算符。Java 中常见的算术运算符有 `+`、`-`、`*`、`/`、`%`、`++`、`--`。其中，`+` 和 `-` 不仅可以参与二元运算，还可以作为一元运算符表示正负号；`+` 在遇到字符串时还具有**拼接字符串**的作用。

| **运算符** | **名称**   | **作用**                   |
| ---------- | ---------- | -------------------------- |
| `+`        | 加法运算符 | 求和；表示正数；字符串拼接 |
| `-`        | 减法运算符 | 求差；表示负数             |
| `*`        | 乘法运算符 | 求积                       |
| `/`        | 除法运算符 | 求商                       |
| `%`        | 取模运算符 | 求余数                     |
| `++`       | 自增运算符 | 当前值加 `1`               |
| `--`       | 自减运算符 | 当前值减 `1`               |

#### `+`、`-`、`*` 的基本含义

`+`、`-`、`*` 的含义相对直观，分别表示加法、减法和乘法。例如，`10 + 3` 的结果是 `13`，`10 - (-3)` 的结果是 `13`，`10 * 3` 的结果是 `30`。需要注意的是，`+10` 和 `-3` 这类写法中，`+` 和 `-` 表示的是数值的正负号，而不是两个数之间的加减运算。

> **注意**：`+` 除了能做数值加法，还可以用于**字符串拼接**。当一个表达式中出现字符串时，`+` 通常不再表示数学加法，而是把两边内容连接起来，形成一个新的字符串。

#### `/` 除法运算

除法运算需要特别注意：**运算结果的类型，取决于参与运算的操作数类型**。如果两个操作数都是整数类型，那么结果仍然按整数处理，小数部分会被直接舍去，而不会四舍五入。例如，`10 / 3` 的结果不是 `3.333...`，而是 `3`。

只要参与除法运算的两个操作数中**有一个是浮点型**，结果就会按照浮点数计算。例如，`10.0 / 3` 的结果才是带小数的值。

还要特别注意下面这种情况：即使最终用 `double` 类型变量接收结果，也不会改变前面已经完成的整数除法过程。例如：

```java
int num9 = 10;
int num10 = 3;
double num11 = num9 / num10;
```

这里 `num9 / num10` 在赋值之前就已经按整数除法计算完毕，结果是 `3`，再赋值给 `double` 时只是变成了 `3.0`，而不是 `3.333...`。

#### `%` 取模运算

`%` 表示**取模**，也可以理解为**求余数**。例如，`10 % 3` 的结果是 `1`，因为 `10 ÷ 3` 的商是 `3`，余数是 `1`。

取模运算可以用下面的公式理解：

```
x % y = x - x / y * y
```

这里要注意，公式中的 `/` 仍然遵循 Java 自身的除法规则；如果 `x` 和 `y` 都是整数，那么这里的除法也是整数除法。

根据这个公式可以看出，余数的符号通常与**被除数**（左边那个数）一致。例如：

- `10 % 3 = 1`
- `-10 % 3 = -1`
- `10 % -3 = 1`

因此，在 Java 中，判断取模结果时，重点要先看左操作数的符号。

> **补充**：取模运算在实际开发中很常见，例如判断奇偶数、控制循环节奏、进行范围轮转等。

#### `++` 与 `--`

`++` 表示**自增 1**，`--` 表示**自减 1**。它们都只能作用在**变量**上，用于在原有值的基础上加一或减一。学习时通常先掌握 `++` 的规则，`--` 与它完全对应，只是把“加 1”变成“减 1”。

`++` 可以写在变量前，也可以写在变量后，即 `++i` 和 `i++`。如果它们是**单独使用**的，例如单独写成一条语句，那么**两者的效果没有区别**，都会让变量本身加 `1`。

```java
int i = 10;
i++;
System.out.println(i); // 11

int k = 10;
++k;
System.out.println(k); // 11
```

但真正需要注意的是：**一旦 `++` 出现在表达式中，前置和后置的含义就不同了。**

- `++` 写在变量前面时，属于**前置自增**，其执行顺序是：**先自增，再参与当前表达式的运算或赋值**。例如：

  ```java
  int i = 10;
  int k = ++i;
  System.out.println("i = " + i); // 11
  System.out.println("k = " + k); // 11
  ```

  这里会先把 `i` 变成 `11`，然后再把这个新值赋给 `k`，因此最后 `i` 和 `k` 都是 `11`。

- `++` 写在变量后面时，属于**后置自增**，其执行顺序是：**先以原值参与当前表达式的运算或赋值，再让变量自身加 1**。例如：

  ```java
  int i = 10;
  int k = i++;
  System.out.println("i = " + i); // 11
  System.out.println("k = " + k); // 10
  ```

  这里先把 `i` 原来的值 `10` 赋给 `k`，然后 `i` 自己再变成 `11`。因此最终 `i` 是 `11`，而 `k` 仍然是 `10`。

可以把两者概括为下面两句话：

- **前置自增** `++i`：先加 `1`，后使用。
- **后置自增** `i++`：先使用，后加 `1`。

`--` 的规则与 `++` 完全一致，只是把“加 `1`”改为“减 `1`”。也就是说，`--i` 是先自减再使用，`i--` 是先使用再自减。

> **练习**：
>
> 【单选题】阅读下面代码，程序的输出结果是下列哪一项？
>
> ```java
> public class ArithmeticTest {
>     public static void main(String[] args) {
>         int a = 10;
>         int b = 3;
>         System.out.println(a / b);
>         System.out.println(a / (double)b);
>         System.out.println(a % -b);
>         System.out.println("结果：" + a + b);
>     }
> }
> ```
>
> - A.
>
>   ```
>   3
>   3.3333333333333335
>   -1
>   结果：13
>   ```
>
> - B.
>
>   ```
>   3
>   3.3333333333333335
>   1
>   结果：103
>   ```
>
> - C.
>
>   ```
>   3.0
>   3.3333333333333335
>   1
>   结果：13
>   ```
>
> - D.
>
>   ```
>   3
>   3
>   -1
>   结果：103
>   ```
>
> 【答案】
>
> - B。`a / b` 是 **整数除法**，结果为 `3`；`a / (double)b` 中有一个操作数是 `double`，因此按 **浮点数除法** 计算，结果为 `3.3333333333333335`；`a % -b` 的结果看 **被除数** `a` 的符号，因此 `10 % -3 = 1`；`"结果：" + a + b` 从左到右执行，前面一旦出现字符串，后面的 `+` 就变成 **字符串拼接**，因此结果是 `"结果：103"`，而不是 `"结果：13"`。

#### 字节码指令

前面学习 `++`、赋值等运算时，如果只停留在源码层面，往往只能记结论；而从 **字节码（bytecode）** 角度看，就能更直观地理解 Java 代码到底是怎样一步一步执行的。这里先不分析构造方法中的指令，只重点关注 `main` 方法中的字节码。

通常可以使用下面的命令查看 `.class` 文件对应的字节码：

```cmd
javap -c ReadClass.class
```

##### 局部变量表与操作数栈

每个方法在执行时，JVM 都会为它分配一块独立的运行空间，这块空间通常称为当前方法的**栈帧**。在这个栈帧中，至少要重点关注两个部分：**局部变量表**和**操作数栈**。

**局部变量表**用于存放方法参数和方法内部定义的局部变量。它可以理解为一组按编号排列的“槽位（slot）”，变量会依次存入这些槽位中。**操作数栈**用于存放运算过程中的临时数据，很多字节码指令都会先把数据压入操作数栈，再从栈顶取出数据继续处理。操作数栈遵循 **后进先出（LIFO）** 的规则，即最后压进去的值最先弹出。

可以先把一个方法运行时的栈帧结构理解为下面这样：

```
┌─────────────────────────────────────┐
│            当前方法的栈帧             │
├─────────────────────────────────────┤
│ 局部变量表（Local Variables）        │
│  slot 0  │ 参数或 this              │
│  slot 1  │ 局部变量                  │
│  slot 2  │ 局部变量                  │
│  ...     │ ...                      │
├─────────────────────────────────────┤
│ 操作数栈（Operand Stack）            │
│                                     │
│               ↑ 栈顶                 │
│          [ 当前参与运算的数据 ]       │
│                                     │
└─────────────────────────────────────┘
```

在后面分析具体代码时，局部变量表中的槽位才会逐步被具体变量占用。例如，`main` 方法是 `static` 方法，因此它没有 `this`，局部变量表的第 `0` 个槽位通常存放参数 `args`；后面定义的局部变量，会依次存入后续槽位中。这也是为什么在分析 `main` 方法字节码时，常常会看到 `istore_1`、`iload_1`、`istore_2` 这类指令。

##### 示例一：`int i = 10;`

源码如下：

```java
public class ReadClass {
    public static void main(String[] args) {
        int i = 10;
    }
}
```

`main` 方法对应的核心字节码如下：

```
0: bipush        10
2: istore_1
3: return
```

这几条指令的含义可以这样理解：

| **指令**    | **作用**                                                    |
| ----------- | ----------------------------------------------------------- |
| `bipush 10` | 将整数常量 `10` 压入操作数栈                                |
| `istore_1`  | 将操作数栈栈顶的整数值弹出，并存入局部变量表的第 `1` 个槽位 |
| `return`    | 方法结束返回                                                |

这段执行过程可以拆成两步来看：

1. 执行 `bipush 10` 时，数字 `10` 先被放入操作数栈。
2. 执行 `istore_1` 时，操作数栈顶部的 `10` 被取出，再存入局部变量表的 `slot 1`，也就是变量 `i` 所在的位置。

可以用一个简单示意图表示这个过程：

```
初始状态
局部变量表：slot0=args，slot1=空
操作数栈：空

执行 bipush 10 后
局部变量表：slot0=args，slot1=空
操作数栈：10 ← 栈顶

执行 istore_1 后
局部变量表：slot0=args，slot1=10
操作数栈：空
```

由此可以看出，Java 中的赋值并不是“直接把 10 放进 i”，而是通常先把数据放到**操作数栈**，再通过存储指令写入**局部变量表**。

##### 示例二：`int j = i;`

源码如下：

```java
public class ReadClass {
    public static void main(String[] args) {
        int i = 10;
        int j = i;
    }
}
```

`main` 方法对应的核心字节码如下：

```
0: bipush        10
2: istore_1
3: iload_1
4: istore_2
5: return
```

相较于前一个例子，这里多了两条关键指令：

| **指令**   | **作用**                                                  |
| ---------- | --------------------------------------------------------- |
| `iload_1`  | 将局部变量表第 `1` 个槽位中的整数值复制一份，压入操作数栈 |
| `istore_2` | 将操作数栈栈顶的整数值弹出，并存入局部变量表第 `2` 个槽位 |

这一过程说明：`int j = i;` 本质上不是“让 `j` 指向 `i`”，而是**把 `i` 当前保存的值复制一份，再存入 `j` 对应的位置**。执行顺序可以概括为：

1. `i = 10` 先通过 `bipush 10` 和 `istore_1` 完成。
2. 执行 `iload_1` 时，把局部变量表中 `i` 的值 `10` 复制到操作数栈。
3. 执行 `istore_2` 时，再把栈顶的 `10` 存入 `j` 对应的槽位。

对应的变化过程如下：

```
执行完 int i = 10 后
局部变量表：slot0=args，slot1=10，slot2=空
操作数栈：空

执行 iload_1 后
局部变量表：slot0=args，slot1=10，slot2=空
操作数栈：10 ← 栈顶

执行 istore_2 后
局部变量表：slot0=args，slot1=10，slot2=10
操作数栈：空
```

> **注意**：`iload_1` 的核心不是“移动”数据，而是**复制一份压栈**。局部变量表中原来的值仍然保留不变。

##### 示例三：`i++;`

源码如下：

```java
public class ReadClass {
    public static void main(String[] args) {
        int i = 10;
        i++;
    }
}
```

`main` 方法对应的核心字节码如下：

```
0: bipush        10
2: istore_1
3: iinc          1, 1
6: return
```

这里最关键的是 `iinc` 指令：

| **指令**    | **作用**                                      |
| ----------- | --------------------------------------------- |
| `iinc 1, 1` | 将局部变量表第 `1` 个槽位中的整数值直接加 `1` |

这条指令非常值得注意。前面我们看到，普通赋值常常要借助“压栈、出栈”完成；而这里的 `i++` 在这种简单场景下，被编译器直接优化成了一条 `iinc` 指令，也就是**直接对局部变量表中的值做自增操作**，并没有显式经过 `iload`、`bipush 1`、`iadd`、`istore` 这样的完整步骤。

它的执行过程可以直接理解为：

```
执行 bipush 10、istore_1 后
局部变量表：slot0=args，slot1=10
操作数栈：空

执行 iinc 1, 1 后
局部变量表：slot0=args，slot1=11
操作数栈：空
```

这说明在 `i++;` 这种**单独成句**的场景中，编译器往往会直接生成更简洁的字节码。

> 结合上面的三个例子，可以先掌握以下几条最基础、最常见的指令：
>
> | **指令**    | **含义**                                                  |
> | ----------- | --------------------------------------------------------- |
> | `bipush`    | 将一个整数常量压入操作数栈                                |
> | `iload_n`   | 将局部变量表中第 `n` 个槽位的整数值复制到操作数栈         |
> | `istore_n`  | 将操作数栈栈顶的整数值弹出，并存入局部变量表第 `n` 个槽位 |
> | `iinc n, c` | 将局部变量表第 `n` 个槽位中的整数值增加 `c`               |
> | `return`    | 方法结束                                                  |
>
> 这些指令正好对应了初学阶段最常见的几类源码动作：**赋值、取值、变量复制、自增和方法结束**。

#### `i = i++` 与 `i = ++i`

当 `++` 出现在**赋值表达式**中，并且左边和右边还是**同一个变量**时，结果往往容易判断错。这里要抓住一个核心：**`++` 不仅会修改变量本身，还会产生一个“表达式结果值”**；前置自增和后置自增的区别，本质上就在于这个“表达式结果值”到底是旧值还是新值。

先看下面这段代码：

```java
public class Test {
    public static void main(String[] args) {
        int i = 10;
        i = i++;
        System.out.println(i); // 10

        int k = 10;
        k = ++k;
        System.out.println(k); // 11
    }
}
```

其中最容易混淆的是：为什么 `i = i++;` 的结果是 `10`，而 `k = ++k;` 的结果是 `11`。

##### `i = i++;` 的执行过程

`i++` 是**后置自增**。后置自增的规则是：**先返回旧值，再让变量自身加 1**。因此，`i = i++;` 不能简单理解为“先加一再赋值给自己”，而应理解为：右边表达式先把 `i` 的旧值拿出来作为结果，然后 `i` 自己加 `1`，最后再把刚才那个旧值重新赋给 `i`。

可以把它近似理解为下面这个过程：

```java
int i = 10;
int temp = i; // 先保存旧值 10
i = i + 1;    // i 先自增为 11
i = temp;     // 再把旧值 10 赋回给 i
System.out.println(i); // 10
```

这里的 `temp` 只是帮助理解的**概念性临时值**，不是说源码一定真的写成了这样，而是为了说明：**后置自增产生的是旧值，这个旧值后面又参与了赋值**，所以把已经自增后的结果覆盖掉了。

从字节码角度看，这个过程会更清楚。`main` 方法中与 `i = i++;` 相关的关键字节码如下：

```
0: bipush        10
2: istore_1
3: iload_1
4: iinc          1, 1
7: istore_1
```

这几条指令可以按顺序理解为：

1. `bipush 10`：把常量 `10` 压入操作数栈。
2. `istore_1`：把 `10` 存入局部变量表第 `1` 个槽位，也就是变量 `i`。
3. `iload_1`：把局部变量表中 `i` 当前的值 **复制一份** 压入操作数栈，此时压入的是旧值 `10`。
4. `iinc 1, 1`：把局部变量表中第 `1` 个槽位的值直接加 `1`，此时 `i` 变成 `11`。
5. `istore_1`：把操作数栈栈顶保存的旧值 `10` 再存回第 `1` 个槽位，于是 `i` 又变回 `10`。

也就是说，`i = i++;` 的关键不是“有没有加一”，而是：**旧值先被保存在操作数栈里，最后又被重新写回去了**。

##### `k = ++k;` 的执行过程

`++k` 是**前置自增**。前置自增的规则是：**先让变量加 1，再返回新值**。因此，`k = ++k;` 可以理解为：`k` 先变成 `11`，然后右边表达式返回的也是 `11`，再把这个 `11` 赋给 `k`，所以最后结果仍然是 `11`。

近似理解如下：

```java
int k = 10;
k = k + 1; // 先自增为 11
int temp = k; 
k = temp;  // 再把新值 11 赋给自己
System.out.println(k); // 11
```

对应的关键字节码如下：

```
15: bipush        10
17: istore_2
18: iinc          2, 1
21: iload_2
22: istore_2
```

这几条指令表示：

1. `bipush 10`、`istore_2`：先把 `10` 赋给变量 `k`。
2. `iinc 2, 1`：直接把局部变量表中第 `2` 个槽位的值加 `1`，此时 `k` 已经变成 `11`。
3. `iload_2`：把此时局部变量表中的新值 `11` 压入操作数栈。
4. `istore_2`：再把操作数栈顶部的 `11` 存回 `k`。

因此，`k = ++k;` 最终保留下来的就是**自增后的新值**。

> **练习**：
>
> 【简答题】阅读下面代码，写出程序的输出结果，并说明原因。
>
> ```java
> public class ArithmeticTest {
>     public static void main(String[] args) {
>         int i = 5;
>         int j = i++ + ++i;
>         int k = 10;
>         k = k++;
>         System.out.println("i = " + i);
>         System.out.println("j = " + j);
>         System.out.println("k = " + k);
>         System.out.println((-10) % 3 + "," + 10 % (-3));
>     }
> }
> ```
>
> 【答案】
>
> - 输出结果是：
>
>   ```
>   i = 7
>   j = 12
>   k = 10
>   -1,1
>   ```
>
>   原因如下：
>
>   1. 初始时 `i = 5`。表达式 `i++ + ++i` 中，`i++` 是 **后置自增**，先取旧值 `5` 参与运算，再让 `i` 变成 `6`；接着 `++i` 是 **前置自增**，先让 `i` 从 `6` 变成 `7`，再取值 `7` 参与运算。因此 `j = 5 + 7 = 12`，同时表达式执行结束后 `i = 7`。
>   2. `k = k++;` 中，`k++` 会先返回旧值 `10`，然后 `k` 自身短暂变成 `11`；但右侧表达式最终返回的是旧值 `10`，再赋值回 `k`，因此最后 `k` 仍然是 `10`。
>   3. `(-10) % 3 = -1`，`10 % (-3) = 1`。这是因为 Java 中取模结果的符号通常与 **被除数** 一致，即左操作数是负数时结果通常为负，左操作数是正数时结果通常为正。

### 关系运算符

**关系运算符**又叫**比较运算符**，用于比较两个值之间的大小关系或相等关系。Java 中常见的关系运算符有 `>`、`>=`、`<`、`<=`、`==`、`!=`。这类运算符最重要的特点是：**运算结果一定是 `boolean` 类型**，也就是说，结果只能是 `true` 或 `false`，不可能是其他类型的值。

| **运算符** | **含义** |
| ---------- | -------- |
| `>`        | 大于     |
| `>=`       | 大于等于 |
| `<`        | 小于     |
| `<=`       | 小于等于 |
| `==`       | 等于     |
| `!=`       | 不等于   |

例如：

```java
int a = 10;
int b = 10;
System.out.println(a > b);  // false
System.out.println(a >= b); // true
System.out.println(a < b);  // false
System.out.println(a <= b); // true
System.out.println(a == b); // true
System.out.println(a != b); // false
```

上面这些表达式的共同点是：虽然参与比较的是整数，但比较完成后得到的结果都不是整数，而是布尔值。

#### `=` 和 `==` 的区别

初学阶段最容易混淆的是 `=` 和 `==`。二者虽然看起来只差一个等号，但含义完全不同：`=` 是**赋值运算符**，表示把右边的值赋给左边的变量；`==` 是**关系运算符**，表示判断左右两个值是否相等。

例如：

```java
int a = 10; // 把 10 赋值给变量 a
System.out.println(a == 10); // 判断 a 中保存的值是否等于 10
```

这里第一句是在“存值”，第二句是在“比较值”，两者不能混为一谈。

> **易错点**：`=` 不是“等于”，而是“赋值”；真正表示“是否相等”的是 `==`。

#### `==` 的本质

对于当前阶段接触到的基本数据类型来说，`==` 比较的是**变量中保存的值是否相等**。只有左右两边的值相同，结果才是 `true`；否则就是 `false`。

例如：

```java
int a = 10;
int b = 10;
System.out.println(a == b); // true
```

这里并不是在比较变量名是否一样，也不是比较它们是不是“同一个变量”，而是在比较 `a` 和 `b` 中保存的数值是否相同。因为二者都保存了 `10`，所以结果是 `true`。

同理，`!=` 则表示“不相等”，只要左右两边的值不同，结果就是 `true`。

#### 关系运算符不能连着比较

在数学中，常见写法如 `1 < 2 < 3` 是可以理解的，但在 Java 中，这种写法是**错误的**。原因在于：`x < y` 先比较后，得到的结果是一个布尔值 `true` 或 `false`；而布尔值不能再继续和整数 `z` 比较大小。

例如下面的写法会编译报错：

```java
int x = 1;
int y = 2;
int z = 3;
// System.out.println(x < y < z); // 编译报错
```

可以这样理解：`x < y` 的结果是 `true`，于是整个表达式会变成类似 `true < z`，而这是不合法的。

如果确实要表达“`x` 小于 `y`，并且 `y` 小于 `z`”，应该拆成两个关系表达式，再用逻辑与 `&&` 连接：

```java
System.out.println(x < y && y < z);
```

这才是 Java 中正确的写法。

> **练习**：
>
> 【单选题】阅读下面代码，程序的输出结果是下列哪一项？
>
> ```java
> public class OperatorExercise {
>     public static void main(String[] args) {
>         int x = 2;
>         int y = 3;
>         int z = 3;
>         System.out.println(x < y);
>         System.out.println(y == z);
>         System.out.println(x != z);
>     }
> }
> ```
>
> - A.
>
>   ```
>   true
>   true
>   true
>   ```
>
> - B.
>
>   ```
>   true
>   false
>   true
>   ```
>
> - C.
>
>   ```
>   false
>   true
>   false
>   ```
>
> - D.
>
>   ```
>   true
>   true
>   false
>   ```
>
> 【答案】
>
> - A。`x < y` 即 `2 < 3`，结果是 `true`；`y == z` 即 `3 == 3`，结果是 `true`；`x != z` 即 `2 != 3`，结果也是 `true`。这道题的重点在于理解：**关系运算符比较的是值，结果一定是布尔值。**

### 逻辑运算符

**逻辑运算符**用于对布尔条件进行组合、取反或比较，常见的有 `&`、`|`、`!`、`^`、`&&`、`||`。这类运算符最核心的特点有两点：**参与逻辑运算的操作数必须是 `boolean` 类型**，并且**运算结果也一定是 `boolean` 类型**。因此，逻辑运算符通常不会单独出现，而是与关系运算符配合使用，用来表达更复杂的判断条件。

| **运算符** | **名称** | **含义**                  |
| ------- | ------ | ----------------------- |
| `&`     | 逻辑与    | 两边都为 `true`，结果才为 `true` |
| `|`      | 逻辑或                     | 两边只要有一边为 `true`，结果就为 `true` |
| `!`     | 逻辑非    | 对布尔值取反                  |
| `^`     | 逻辑异或   | 两边结果不同，结果为 `true`       |
| `&&`    | 短路与    | 结果规则与 `&` 相同，但存在短路      |
| `||`                       | 短路或                         | 结果规则与 ` | ` 相同，但存在短路 |


#### `&`：逻辑与

`&` 表示“并且”。只有左右两个条件都成立，整个表达式的结果才是 `true`；只要有一个条件不成立，结果就是 `false`。

例如，某用户只有在“年龄大于等于 18 岁”并且“已完成实名认证”时，才允许开通某项服务：

```java
int age = 20;
boolean verified = true;
System.out.println(age >= 18 & verified); // true
```

这里左边条件和右边条件都为 `true`，所以整体结果为 `true`。

#### `|`：逻辑或

`|` 表示“或者”。只要左右两个条件中有一个成立，结果就是 `true`；只有两边都不成立时，结果才是 `false`。

例如，某活动规定：新用户或者会员用户都可以领取优惠券：

```java
boolean newUser = false;
boolean vipUser = true;
System.out.println(newUser | vipUser); // true
```

虽然 `newUser` 是 `false`，但 `vipUser` 是 `true`，所以整体结果仍然是 `true`。

#### `!`：逻辑非

`!` 表示“取反”。如果原来是 `true`，取反后就是 `false`；如果原来是 `false`，取反后就是 `true`。它是一元运算符，只作用于一个布尔值。

例如：

```java
boolean loggedIn = false;
System.out.println(!loggedIn); // true
```

这里 `loggedIn` 表示“是否已登录”，其值为 `false`；取反后得到 `true`。

#### `^`：逻辑异或

`^` 表示“异或”，可以理解为“两边不同才为真”。也就是说，左右两个布尔值**不一样**时，结果是 `true`；如果两边一样，结果就是 `false`。

| **左边** | **右边** | **结果** |
| -------- | -------- | -------- |
| `true`   | `true`   | `false`  |
| `false`  | `false`  | `false`  |
| `true`   | `false`  | `true`   |
| `false`  | `true`   | `true`   |

例如：

```java
boolean a = true;
boolean b = false;
System.out.println(a ^ b); // true
```

因为两边不一样，所以结果为 `true`。

#### `&&`：短路与

`&&` 与 `&` 的**最终运算结果完全相同**，都是“只有两边都为 `true`，结果才为 `true`”。它们的区别不在结果，而在**执行过程**：`&&` 存在**短路现象**。

所谓短路，就是当左边表达式已经足以确定整个结果时，右边表达式就不再执行。对于 `&&` 来说，只要左边是 `false`，整个结果一定是 `false`，因此右边没有继续执行的必要。

例如：

```java
int score = 55;
int count = 1;
System.out.println(score >= 60 && count++ > 0); // false
System.out.println(count); // 1
```

这里左边 `score >= 60` 的结果已经是 `false`，因此整个 `&&` 表达式不可能为 `true`，右边 `count++ > 0` 就不会执行，所以 `count` 仍然是 `1`。

如果把 `&&` 换成 `&`，那么即使左边已经是 `false`，右边仍然会执行。

> **结论**：`&&` 比 `&` 更常用，因为它有短路特性，通常效率更高，也更符合日常条件判断的习惯。

#### `||`：短路或

`||` 与 `|` 的**最终运算结果也完全相同**，都是“只要有一边为 `true`，结果就是 `true`”。区别同样在执行过程：`||` 也存在短路现象。

对于 `||` 来说，只要左边已经是 `true`，那么整个表达式必定为 `true`，右边就不需要再执行。

例如：

```java
boolean admin = true;
int times = 2;
System.out.println(admin || times++ > 3); // true
System.out.println(times); // 2
```

这里左边 `admin` 已经是 `true`，所以整个表达式直接成立，右边不会执行，`times` 的值不会发生变化。

#### `&` 与 `&&`、`|` 与 `||` 的区别

学习逻辑运算符时，最容易混淆的就是这两组符号。它们要分成“结果”和“执行过程”两层来看：

| **运算符** | **结果规则** | **是否短路** |
| ---------- | ------------ | ------------ |
| `&`        | 逻辑与       | 否           |
| `&&`       | 逻辑与       | 是           |
| `|`        | 逻辑或       | 否           |
| `||`       | 逻辑或       | 是           |

也就是说，`&` 和 `&&` 的真假规则一样，`|` 和 `||` 的真假规则也一样；真正不同的是，短路运算符在某些情况下不会执行右边表达式。

虽然多数情况下更推荐使用 `&&` 和 `||`，但 `&` 和 `|` 也并不是没用。当需求要求**无论左边结果如何，右边都必须执行**时，就不能用短路运算符，而要用 `&` 或 `|`。


> **练习**：
>
> 【单选题】阅读下面代码，程序的输出结果是下列哪一项？
>
> ```java
> public class LogicTest1 {
>     public static void main(String[] args) {
>         int n = 5;
>         boolean result = n > 3 && n++ > 10;
>         System.out.println(result);
>         System.out.println(n);
>     }
> }
> ```
>
> - A.
>
>   ```
>   true
>   6
>   ```
>
> - B.
>
>   ```
>   false
>   5
>   ```
>
> - C.
>
>   ```
>   false
>   6
>   ```
>
> - D.
>
>   ```
>   true
>   5
>   ```
>
> 【简答题】阅读下面代码，写出程序的输出结果，并说明原因。
>
> ```java
> public class LogicTest2 {
>     public static void main(String[] args) {
>         boolean a = true;
>         boolean b = false;
>         int x = 1;
> 
>         System.out.println(a ^ b);
>         System.out.println(!a || x++ > 0);
>         System.out.println(x);
>     }
> }
> ```
>
> 【答案】
>
> - C。左边 `n > 3` 的结果是 `true`，因此 `&&` 不会短路，右边 `n++ > 10` 会继续执行。此时先使用 `n` 的旧值 `5` 参与比较，`5 > 10` 为 `false`，然后 `n` 自增为 `6`。所以整个表达式结果为 `false`，最终 `n` 的值为 `6`。
>
> - 输出结果是：
>
>   ```
>   true
>   true
>   2
>   ```
>
>   原因如下：
>
>   1. `a ^ b` 是逻辑异或，两边不同则结果为 `true`，因此第一行输出 `true`。
>   2. `!a` 的结果是 `false`，因此第二行变成 `false || x++ > 0`。由于左边是 `false`，`||` 不会短路，右边继续执行。
>   3. `x++ > 0` 比较时先使用旧值 `1`，`1 > 0` 为 `true`，然后 `x` 自增为 `2`。
>   4. 所以第二行整体结果为 `true`，第三行输出 `2`。
