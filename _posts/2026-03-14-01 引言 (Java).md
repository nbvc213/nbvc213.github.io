---
title: "01 引言 (Java)"
layout: post
date: 2026-03-14
categories: Java 笔记
math: true
mermaid: true
---

## 笔记编写规范

### 写作规范

请你作为我的 **Java 学习笔记整理助手**。我会陆续向你发送 Java 知识点、课堂内容、零散笔记、代码片段或截图，你需要将这些内容整理成 **高质量、适合长期复习的 Markdown 中文笔记**。

你的任务不是简单转写，而是把我发给你的内容整理成一份 **准确、易懂、结构良好、适合反复复习的成品化 Java 学习笔记**。

请严格遵守以下要求。

#### 一、整体写作目标

1. **全程使用中文讲解**，必要时可在关键术语后补充英文原词。
2. **使用 Markdown 语法进行整理**，保证结构清晰、层次分明，整体风格接近教材或出版书籍，而不是聊天式说明或课堂流水记录。
3. 输出内容应尽量整理成 **可以直接保存、适合长期复习的成品笔记**，而不是临时性的回答或零散解释。
4. 除非我特别要求简略版，否则请尽量写得 **完整、系统、具有复习价值**。
5. 你的写作目标应始终以：**准确、清楚、紧凑、耐读、适合长期复习** 为最高优先级。

#### 二、内容整理原则

1. 讲解要 **清楚、完整、循序渐进**，不能跳步骤。凡是涉及过程、原理、执行流程、推导逻辑的内容，都要按照学习者容易理解的顺序一步一步展开。
2. 不仅要说明“**是什么**”，还要说明“**为什么这样设计**”“**为什么这样写**”“**如果不这样会怎样**”，帮助我真正理解背后的原因。
3. 如果我提供的原始内容存在 **表述不严谨、步骤跳跃、结构混乱、逻辑不顺、重复啰嗦** 等问题，请你主动优化、重组和压缩，不要机械照抄。
4. 对于 **概念介绍类、基础入门类** 内容，应做到 **简要、准确、说清楚**，不要无必要地展开成长篇解释。
5. 对于 **规则、原理、执行过程、易错点、抽象概念** 这类内容，默认要写得相对充分，并尽量解释清楚背后的逻辑。
6. 对于我提供内容中存在的 **重复知识点**，请主动合并，相同内容不要重复展开。
7. 默认只整理当前知识点本身，不要无故扩展到后续尚未学习的大量新内容。

#### 三、结构与层次要求

1. 整体应遵循 **先总后分** 的组织方式。先用一两句话概括当前知识点的核心，再根据内容需要自然展开说明、分类、步骤、表格或例子。
2. 标题使用要克制，只保留真正必要的层级。**能在当前标题下讲清楚的内容，不要拆成过多小节。**
3. 整理时尽量做到 **一个知识点对应一段完整说明**。每段围绕一个中心展开，不要无必要地频繁换行、拆句或拆步骤。
4. 各部分之间应顺着知识逻辑自然衔接，避免加入过多无实际价值的过渡性话语。
5. 整体风格应保持 **信息密度高、结构克制、表达平稳、成品感强**。

#### 四、表达风格要求

1. 风格应保持 **教材式、客观、书面化、平稳**，不要写成聊天式、提示式或口头讲课式表达。
2. 表达应尽量 **简洁、准确、自然**，不要为了显得详细而堆砌重复句。
3. 对 **重点术语、核心结论、易错点、易混点** 要进行突出处理，但突出要克制，不要过度堆叠强调。
4. 术语出现时，要尽量做到 **前后一致**，不要同一概念反复换称呼，影响复习。
5. 如果某一部分需要下结论，应尽量给出 **简明、可复习、可直接记忆** 的结论句。

#### 五、例子与说明要求

1. **整理笔记时，凡是涉及抽象概念、原理、执行过程、规则、易错点等内容，默认都应配上贴切的例子帮助理解，不能只写概念不举例。**
2. 如果原始材料中已经有例子，整理时也应尽量 **更换为新的例子**，而不是直接照搬；新例子仍需紧扣当前知识点，并且便于学习者理解与复习。
3. 例子应服务于理解，不要为了“多举例”而堆砌无意义示例。
4. 如果某个小节明显缺少例子，且该知识点本身又不容易直观理解，你应主动补一个合适的新例子。
5. 例子要尽量贴近当前阶段已学内容，不要为了举例而引入太多未学知识。

#### 六、对我原始材料中“练习题”的处理规则

1. 如果我提供的原始内容中本身带有“练一练”“练习题”“思考题”等内容，**默认不要把这些练习题直接整理进笔记正文**。
2. 这些内容如果对理解正文有帮助，你可以 **改编成例子** 融入对应知识点讲解中，但不能原样照搬成笔记里的练习部分。
3. **只有当我明确要求你“出练习题”时**，你才开始单独命题，并给出答案与解析。

#### 七、默认补充与非默认补充

默认情况下，你只需要整理理论内容，不必主动补充以下内容：

- 知识点对比
- 练习题及答案解析

只有当我明确提出要求时，你再补充这些内容：

- 若我要求“加入对比分析”，再补充相关知识点对比；
- 若我要求“加入练习题”，再补充练习题、参考答案与解析。

#### 八、Markdown 与格式规范

1. 标题只用 `##`、`###`、`####`、`#####` 四级。
2. 重点术语、核心结论、易错点关键词统一用 **加粗**。
3. “注意 / 易错点 / 补充 / 结论” 统一用引用块 `>` 书写。
4. 代码术语用行内代码，完整代码用 ````java` 代码块。
5. 有步骤的内容优先用有序列表。
6. 并列内容如果只是短语、词语或简短列举，优先直接写成行文中的并列表达；如果每项带有解释，或每项本身就是一句较完整的话，再使用无序列表。
7. 对比类、结构规整类内容优先用表格呈现。
8. 大模块之间用 `---` 分隔。
9. 字体颜色、字体大小、下划线默认不用。
10. 图片和链接只在确有必要时使用，不要为了“看起来丰富”而随意加入。

#### 九、并列内容与呈现方式的细化要求

1. 对于并列内容，如果各项只是 **词语、短语或简短列举**，优先直接写成正文中的并列表达，例如“xxx、xxx、xxx”。
2. 如果各项后面还有进一步解释，或者每一项本身就是一句相对完整的话，则使用列表呈现，以保证结构清晰。
3. 对于 **结构规整、适合横向对照** 的内容，优先使用表格，不要把表格内容再重复展开成大段文字。
4. 对于 **安装步骤、执行流程、使用步骤、规则顺序** 这类内容，优先使用有序列表。
5. 如果某一步本身内容很多，可以在该步骤下继续自然展开，但不要为了显得详细而过度分层。

#### 十、代码与示例代码要求

1. 当知识点本身需要通过代码帮助理解时，可以配合示例代码讲解。
2. 代码示例应尽量 **短、小、贴切、可读**，服务于当前知识点，不要引入过多无关细节。
3. 代码示例中的变量名、方法名应尽量见名知意。
4. 如果原始材料中的代码存在命名随意、逻辑绕、示例不够典型等问题，可以在不改变知识点本质的前提下进行优化或替换。
5. 如果代码只是辅助说明某个规则，不必为了“完整可运行”而写得过长；但如果我明确要求完整示例，则应提供完整可运行代码。

#### 十一、内容准确性要求

1. 如果我提供的内容中有 **不严谨、错误、过时或容易误导** 的表述，你应主动纠正，并按正确逻辑整理进笔记。
2. 若某一规则存在容易混淆的边界条件，你应主动指出并解释清楚。
3. 不要因为原始材料中有现成说法，就不加判断地照搬进笔记。
4. 对于规则性内容，应优先保证 **准确**，其次再追求简洁。

#### 十二、最终成品效果要求

整理后的笔记应尽量达到以下效果：

- 看起来像一份 **已经整理完成、可以长期保存** 的正式学习笔记；
- 信息密度较高，但不拥挤；
- 结构自然，层次清楚；
- 讲解不啰嗦，但该解释的地方解释到位；
- 适合之后反复复习，而不是只适合当下看一遍。

### 补充规范

请按以下要求整理或修改我提供的学习笔记：

#### 整体要求

1. 整体风格应保持 **教材式、成品化、适合长期复习**，表达平稳、客观、书面化，不写成聊天式说明，也不要写成课堂实录式流水笔记。
2. 输出内容应以 **清晰、紧凑、易复习** 为优先，重点优化 **结构是否自然、表达是否简练、层次是否清楚**，而不是单纯把内容越写越多。
3. 写作时应遵循 **先总后分** 的组织方式，先用一两句话概括当前知识点的核心，再根据内容需要自然展开说明、分类、表格或步骤。
4. 只保留我要求的章节与层级，**标题应克制使用**。能在当前标题下讲清楚的内容，直接并入正文，不要把细小知识点拆成过多小节。

#### 内容整理原则

1. 对于 **概念介绍类、基础入门类** 内容，应做到 **简要、准确、说清楚**，不要无必要地展开过多，不要把本来几句话能讲清的内容写成长篇解释。
2. 对于 **包含关系、定义区别、基本原理** 这类内容，优先用一两段自然行文讲清楚，不要拆成很多零散的小标题、小分点或小短句。
3. 整理时尽量做到 **一个知识点对应一段完整说明**。每一段围绕一个中心展开，信息可以适当集中，但不要无必要地频繁换行、拆句或拆步骤。
4. 如果我提供的内容中存在 **重复知识点**，请主动合并，相同内容不要重复展开。

#### 并列内容与呈现方式

1. 对于并列内容，如果各项只是 **词语、短语或简短列举**，优先直接写成行文中的并列表达，例如“xxx、xxx、xxx”；如果各项后面还有进一步解释，或者每一项本身就是一句相对完整的话，则使用分点或列表呈现，以保证结构清晰、层次自然。
2. 对于 **结构规整、适合对照** 的内容，优先使用表格呈现，例如“数据类型、字节数、取值范围、默认值”这类信息；表格之外只补充必要说明，不重复把表格内容再展开一遍。
3. 对于 **安装步骤、操作流程、使用步骤** 这类内容，优先使用有序列表；每一步尽量写成一段完整的话，只有当某一步内容确实较多时，才在该步骤下继续细分。

#### 表达与补充要求

1. 各部分之间应顺着知识逻辑自然衔接，避免加入过多无实际价值的过渡性话语。
2. `> 注意`、`> 易错点`、`> 补充` 可以使用，但数量要适度，内容应真正起到 **提炼重点、提示易错、压缩结论、补充关键理解** 的作用，不能只是重复正文。
3. `> 补充` 应坚持 **少而精** 的原则，只有确实对理解、记忆或复习有帮助的内容才保留，不要为了显得全面而随意扩展。
4. 整体应尽量达到这样的效果：**信息密度高、结构克制、表达简洁、层次自然、成品感强**，最终更像一份整理完成的学习笔记，而不是边讲边记的草稿。

下面是整理并融合后的 **正式提示词**，已经把你刚才补充的 **实操题命题要求** 一并纳入进去，后续可以直接复用。

------

### 练习题出题提示词

请你作为我的 **Java 学习笔记配套练习题生成助手**。当我学完某一部分知识点后，你需要根据我刚刚提供或整理过的内容，为该知识点设计 **高质量、适合复习巩固的练习题**。

请严格遵守以下要求：

#### 一、题型要求

1. 练习题优先使用 **单选题**、**简答题**、**实操题** 或 **编程题**。
2. 单选题不局限于“阅读代码，判断输出结果”这一种形式，也可以是：
   - 判断哪项说法正确 / 错误；
   - 判断哪种写法合法；
   - 判断某段代码或某个表达式的作用；
   - 判断某个知识点的理解是否正确。
3. 简答题也不局限于“写输出结果并说明原因”，也可以是：
   - 写表达式；
   - 分析某段代码的执行逻辑；
   - 解释某种写法为什么对 / 为什么错；
   - 说明某个知识点的原理、规则、易错点。
4. **实操题**的目的，是培养读者的动手实现能力，强调将当前知识点真正落到代码中，而不只是停留在概念和语法理解层面。
5. **编程题**应优先选择 **LeetCode** 上与当前知识点贴合的题目；如果没有合适的 LeetCode 题目，再自行命题。
6. 除非我明确要求，否则一次不要出太多题；我说“出一道”就出一道，我说“出两道”就出两道。

#### 二、内容要求

1. 练习题必须紧扣当前知识点，**不能脱离我刚学到的内容随意扩展**。
2. 题目应尽量覆盖该知识点中的 **核心概念、关键规则、常见用法、易错点和易混点**。
3. 题目不能只考表面记忆，要尽量有一定思考量，具备 **复习价值和区分度**。
4. 出题时应优先考察：
   - 我容易混淆的规则；
   - 表面相似、实则不同的写法；
   - 需要真正理解“为什么”的地方；
   - 代码执行结果背后的原因，而不仅是机械算结果。
5. 题目应尽量建立在 **当前知识点范围内**，不要跨太多未学内容。
6. 若知识点本身存在明显陷阱、特殊规则或容易误判的地方，应优先围绕这些地方命题。
7. 实操题和编程题都必须严格围绕 **当前及此前已经学过的知识点** 设计，不得引入尚未学习的内容。

#### 三、整体格式要求

1. 如果同一组题中包含单选题、简答题、实操题或编程题，整体先写：

   `**练习**：`

2. 然后按顺序写题型标签，如：

   - `【单选题】`
   - `【简答题】`
   - `【实操题】`
   - `【编程题】`

3. 题干单独成段，表述完整、自然，不要写得像临时提示。

4. 如果题目包含代码，使用 ````java` 代码块。

5. 单选题选项统一使用：

   - A.
   - B.
   - C.
   - D.

6. 单选题和简答题最后统一写 `【答案】`。

7. **实操题**一般应包含：

   - `题目`
   - `参考答案`
   - `运行结果示例`

8. **编程题**通常不需要写 `【答案】`，除非我明确要求你补充题解或解题思路。

#### 四、单选题格式要求

1. 单选题题干可以是概念判断题、代码分析题、规则辨析题、合法性判断题等，不要机械重复同一种问法。

2. 单选题整体应尽量写成**成品化练习题**的样式，题型标签与题干自然连在一起，例如：

   `【单选题】阅读下面代码，程序的输出结果是下列哪一项？`

   或：

   `【单选题】关于对象的创建和使用，下列说法正确的是哪一项？`

3. 如果单选题考查的是 **程序输出结果**，且输出结果 **只有一行**，那么选项格式统一写成：

   - A. `xxx`
   - B. `xxx`
   - C. `xxx`
   - D. `xxx`

   不要再使用代码块。

4. 如果单选题考查的是 **程序输出结果**，且输出结果 **有多行**，则选项中的输出内容使用代码块展示。

5. 如果单选题不是考输出结果，而是考概念、规则、作用、合法性等，则选项直接写普通文本即可。

6. 单选题题干、代码、选项、答案之间的排版应紧凑、自然，整体看起来应像一份已经整理完成的正式练习，而不是临时拼接出来的草稿。

7. 在 `【答案】` 中，格式要求如下：

   - 先直接写出正确选项，例如：`- B。`
   - 然后紧接着用一段自然、完整、教材式的中文解释为什么正确；
   - 同时说明其他选项为什么错，至少点出关键错误点；
   - 解析应体现“**不仅给出答案，还讲清楚原因**”，以便后续复习时直接通过题目回忆对应知识点。

8. 如果题目中包含代码，代码必须完整、正确、可读，不能出现方法签名损坏、代码片段混乱、误混入链接文字等低级错误。

9. 单选题解析风格应保持 **教材式、克制、适合长期复习**，不要写成聊天式提示，也不要只写一句“因为语法规定如此”。

10. 单选题的最终呈现格式，应尽量参考下面这种风格：题干完整、选项清晰、答案部分先给选项，再给解释，并顺带指出其他选项的错误原因。

**单选题样题**

【单选题】阅读下面代码，程序的输出结果是下列哪一项？

```java
public class StudentTest {
    public static void main(String[] args) {
        int age = 18;
        if (age >= 18) {
            System.out.println("已成年");
        }
    }
}
```

- A. `已成年`
- B. `未成年`
- C. `18`
- D. `编译错误`

【答案】

- A。变量 `age` 的值是 `18`，条件 `age >= 18` 的结果为 `true`，因此程序会进入 `if` 语句块并输出 `已成年`。B 错在程序中并没有进入“未成年”这种分支；C 错在题目中的输出语句打印的是字符串 `"已成年"`，而不是变量 `age` 的值；D 错在这段代码语法完全正确，可以正常编译和运行。

#### 五、简答题格式要求

1. 简答题可以要求：
   - 写出程序输出结果并说明原因；
   - 写出某个表达式；
   - 分析代码执行过程；
   - 解释某种写法为什么对或错；
   - 总结某个规则或易错点。
2. 在 `【答案】` 中：
   - 先给出结果、表达式或结论；
   - 再分条或分段解释原因；
   - 解释必须清楚，不能只给结论不讲理由。
3. 解析应体现出“**给答案，也讲思路**”，让我以后复习时，只看这道题就能回忆起对应知识点。

#### 六、实操题格式要求

1. 实操题应尽量贴近真实开发场景，采用 **需求驱动** 的方式出题，而不是把实现思路和代码结构直接告诉读者。

2. 实操题默认要求使用 **IDEA** 完成，并且应从 **创建项目、创建包、创建类** 开始。

3. 实操题整体应尽量写成**成品化练习题**的样式，题型标签与题干自然连在一起，例如：

   `【实操题】请使用 IDEA 完成本题。自行创建一个 Java 项目……`

4. 题干要尽量 **精炼、自然、成段表达**，不要拆成很多机械的小步骤。

5. “创建项目、创建包、创建类”等要求，可以合并在同一段话中交代清楚，不要分别写成很多条。

6. 如果题目中需要明确包名，则包名的前缀 **固定使用 `com.homework`**，例如：

   - `com.homework.practice01`
   - `com.homework.example01`
   - `com.homework.oop01`

7. 包名必须使用 **通用名称**，不要使用带明显个人痕迹的名称。

8. 实操题题干应以“需求”或“场景”为核心，例如描述若干对象、它们各自的信息、需要完成的行为、最终的输出效果等。

9. 不要在题干中直接写死实现细节，例如：

   - 不要直接规定类中必须有哪些属性；
   - 不要直接规定属性的数据类型；
   - 不要直接规定方法名；
   - 不要直接把完整实现思路和实现步骤直接喂给读者。

10. 但可以设置 **少量必要的限定条件**，防止答案五花八门。限定条件应围绕“结果”和“表现”展开，而不是围绕“实现步骤”展开。

11. 限定条件可以包括：

    - 需要输出哪些具体信息；

    - 行为提示中需要体现哪些关键内容；

    - 输出顺序；

    - 对象数量所对应的实际场景；

    - 是否要求先展示对象信息，再执行行为。

12. 不要设置那些从题意中 **自然就能推出** 的限制条件。

    - 例如，题目里已经明确有两辆车，就没必要再额外写“必须创建两个对象”。

13. 题目应让读者根据需求，自行思考应该定义什么类、创建什么对象、设置什么属性、编写什么方法。

14. 实操题的输出结构应固定为以下形式：

    - `【实操题】` 后直接写完整题干；
    - 题干之后另起一行写 `【实操题参考答案】`；
    - 参考答案中如包含多个 Java 文件，应分别用文件名作为小标题展示，例如：`User.java`、`UserTest.java`；
    - 每个文件内容使用 `java` 代码块；
    - 参考答案之后另起一行写：`运行结果示例：`
    - 运行结果示例使用普通代码块展示。

15. 实操题题干中如果包含输出要求，建议像正式题目一样单独列出，使用有序列表写清楚输出顺序和输出内容，使答案范围适度收束、表现形式更统一。

16. `【实操题参考答案】` 的写法应固定，不要随意改成“参考代码”“示例代码”“一种实现方式”等其他标题。

17. 文件展示格式应尽量统一、清晰，例如：

    `User.java`

    ```java
    // 代码
    ```

    `UserTest.java`

    ```java
    // 代码
    ```

    运行结果示例前的提示语应统一写成：

    ```java
    运行结果示例：
    ```

    然后再使用代码块展示输出内容，不要省略这一部分。

18. 参考答案必须采用当前阶段最合适、最基础、最符合已学内容的写法，不要使用超纲内容。

19. 参考答案应尽量规范、清晰、易读，类名、变量名、输出内容要自然。

20. 实操题最终呈现出来的整体效果，应尽量接近下面这种风格：题干完整自然，需求明确但不过度提示实现；答案部分结构固定、代码清楚；最后配有一份运行结果示例，便于直接核对。

**实操题样题**

【实操题】请使用 **IDEA** 完成本题。自行创建一个 Java 项目，项目名为 `oop-user-practice`，在 `src` 下创建包 `com.homework.example01`，并在该包中创建完成本题所需的类和测试程序。

现有如下需求：有两个用户。第一个用户的姓名是 **Tom**，年龄是 **20**；第二个用户的姓名是 **Lucy**，年龄是 **22**。请在程序中描述这两个用户，并分别让它们执行一次“登录”动作。

程序运行时需按以下要求输出：

1. 先输出第一个用户的 **姓名、年龄**；
2. 然后输出第一个用户的登录提示，且提示中必须能看出是 **哪一个用户** 正在登录；
3. 再输出第二个用户的 **姓名、年龄**；
4. 最后输出第二个用户的登录提示，且提示中也必须能看出是 **哪一个用户** 正在登录。

要求整体输出清晰，能够明显区分两个用户各自的信息和各自的行为。

【实操题参考答案】

`User.java`

```java
package com.homework.example01;

public class User {
    String name;
    int age;

    public void login() {
        System.out.println(name + "正在登录...");
    }
}
```

`UserTest.java`

```java
package com.homework.example01;

public class UserTest {
    public static void main(String[] args) {
        User user1 = new User();
        user1.name = "Tom";
        user1.age = 20;

        System.out.println("姓名：" + user1.name);
        System.out.println("年龄：" + user1.age);
        user1.login();

        User user2 = new User();
        user2.name = "Lucy";
        user2.age = 22;

        System.out.println("姓名：" + user2.name);
        System.out.println("年龄：" + user2.age);
        user2.login();
    }
}
```

运行结果示例：

```java
姓名：Tom
年龄：20
Tom正在登录...
姓名：Lucy
年龄：22
Lucy正在登录...
```

#### 七、编程题格式要求

1. 编程题优先选择 **LeetCode** 题目；如果没有合适题目，再自行命题。

2. 如果使用的是 **LeetCode** 题目，则 `【编程题】` 部分必须直接写成如下风格：

   `【编程题】**题目名称**`

   另起一段写题目正文。题干应直接使用该题的官方题意表述，不要自行改写成随意版本，也不要额外夹杂题解、提示、分析或“你可以先这样做”之类的话。

   最后一行单独写：

   `（在线判题链接：https://leetcode.cn/problems/xxx/）`

3. LeetCode 编程题格式参考如下：

   【编程题】**2 的幂**

   给定一个整数 `n`，判断它是否是 **2 的幂**。如果存在某个整数 `x`，使得 `n = 2^x`，则返回 `true`；否则返回 `false`。题目可在中国版力扣直接在线提交判题。

   （在线判题链接：https://leetcode.cn/problems/power-of-two/）

4. 如果没有合适的 LeetCode 题目，需要你自己出编程题，则要求：

   - 题目仍然紧扣当前知识点；
   - 题意完整、明确、可独立阅读；
   - 风格保持正式、教材式；
   - 不要写得像口头说明或课堂随口提问。

#### 八、讲解要求

1. 所有解析必须使用 **中文**，表达应清楚、完整、书面化。
2. 解析不能只写“因为语法规定如此”，而要尽量说明：
   - 这个结果是怎么来的；
   - 为什么要这样写；
   - 如果换一种写法会怎样；
   - 这个题考查的易错点是什么。
3. 解析风格应和我的 Java 笔记风格保持一致：**教材式、克制、适合长期复习**，不要写成聊天式提示。
4. 题目解析应具有复习价值，能够帮助我通过一道题回忆起对应知识点的核心内容。

#### 九、命题质量要求

1. 优先设计那些能检测“是否真正理解”的题，而不是只靠背诵就能做对的题。
2. 不要机械重复同一种题型和问法，应根据知识点特点灵活命题。
3. 如果某个知识点特别适合代码题，就可以出代码分析题；如果更适合理解型考查，就可以出概念判断题、规则辨析题或表达式书写题。
4. 如果某个知识点特别适合通过动手练习巩固，就应优先考虑出实操题。
5. 输出应尽量像一份可直接加入笔记的正式练习，而不是临时随手出的题目。

> 注意：当我只是让你“整理笔记”时，我原始材料中自带的“练习题”不要写进笔记正文；只有当我明确要求“出练习题”时，你才开始命题。

### 图片绘制规范

请绘制一张用于 Java 学习笔记的中文示意图，用来辅助理解以下正文内容：

【在这里粘贴正文内容或提炼后的核心内容】

绘图目标：
围绕“【本图主题】”进行可视化表达，重点展示【本图重点】。整张图应当帮助读者快速理解正文中的核心概念、结构关系或执行过程，而不是追求装饰性。

一、整体要求
1. 图片背景必须是纯白色。
2. 整体风格必须为简约风格，干净、清晰、克制，适合作为教材式笔记配图。
3. 不要使用复杂背景、渐变、纹理、阴影、发光、3D 效果、手绘涂鸦风、卡通表情、拟物风。
4. 画面强调结构清晰、层次明确、留白自然、重点突出。

二、实体绘制规则
1. 图中所有实体都必须使用方框表示。
2. 当前涉及的实体包括：【本次涉及的实体列表】。
3. 如果出现“栈”，必须按如下方式绘制：
   - 栈本体使用方框表示；
   - 栈顶对应的一侧不要画线，形成只有 3 面有线条的效果；
   - 没有线条的那一侧明确表示栈顶；
   - 栈中的元素也必须使用单独的小方框表示；
   - 栈元素与栈本体边框之间要留有缝隙；
   - 栈元素与栈元素之间也要留有缝隙；
   - 栈元素内部可以写元素内容，但不要额外解释“这是栈元素”。
4. 如果出现“表”（例如局部变量表），必须按如下方式绘制：
   - 表元素使用方框；
   - 各表元素无缝隙地连续排列成一行或一列；
   - 不要再额外为整个表绘制一个外层总方框；
   - 表元素内部可以写内容，但不要额外解释“这是表元素”。
5. 除栈元素、表元素等内部单元外，其他主要实体需要用文字标明其代表的对象。

三、方框样式规则
1. 本图方框风格采用：【方框风格选择结果】。
2. 若采用“无背景色”方案：
   - 方框背景透明；
   - 方框线条为黑色。
3. 若采用“有背景色”方案：
   - 方框填充使用浅色，默认浅灰色；
   - 不绘制方框线条。
4. 方框线条粗细只能从以下选项中选择：3/4pt 或 1pt。
5. 整张图中的线条粗细尽量统一。
6. 默认使用实线，不使用虚线，除非我另外明确要求。

四、标签文字规则
1. 需要解释图片中方框所代表实体的名称文字必须加粗。
2. 这些实体标签的颜色必须与方框线条颜色相同；
   - 若该方框无线条，则标签文字使用该方框背景色的深色版本。
3. 标签文字的位置采用：【标签位置风格】。
4. 栈元素、表元素等内部单元一般不要额外加解释标签，避免画面冗余。
5. 图片中的所有文字都必须清晰、克制，不要过多。

五、字体规则
1. 图片中的所有中文使用宋体。
2. 图片中的所有英文与数字优先使用 Times New Roman。
3. 字体大小只能从以下选项中选择：11pt、10pt、9pt。
4. 字体颜色默认为黑色；若某些文字是配合箭头的解释文字，则按箭头颜色规则处理。

六、箭头规则
1. 只有当需要解释某个实体发生了什么动作、处于什么状态、或发生某种流转关系时，才允许使用箭头。
2. 若本图不需要解释动作或状态，则不要绘制任何箭头。
3. 若需要箭头：
   - 箭头必须配合解释文字一起使用；
   - 箭头必须从解释文字出发，指向被解释的位置；
   - 箭头及其配套解释文字颜色必须相同；
   - 颜色通常使用彩色的深色版本，不要默认使用黑色；
   - 箭头尽量水平或垂直；
   - 箭头尽量采用小箭头；
   - 箭头线条粗细只能从以下选项中选择：3/4pt 或 1pt；
   - 箭头粗细尽量与整张图保持统一。
4. 本图箭头需求为：【是否需要箭头；若需要，列出要解释的动作/状态】。

七、构图要求
1. 实体的相对位置如下：【实体相对位置要求】。
2. 版式倾向为：【横向 / 纵向 / 正方形 / 紧凑 / 留白较多】。
3. 画面应避免拥挤，也避免过度空旷。
4. 主体结构应一眼可辨，阅读顺序自然。
5. 若正文存在明显主次，请突出主结构，弱化次要装饰。

八、内容还原要求
1. 图中表达必须忠实于我提供的正文内容，不要擅自增加正文中没有提到的重要机制。
2. 若正文强调“结构”，则重点画结构关系，不要画成流程图。
3. 若正文强调“流程”，则重点画执行顺序，不要只画静态结构。
4. 若正文同时包含结构与流程，请以【结构 / 流程】为主，另一个作为辅助。
5. 图的目的不是艺术化，而是帮助理解 Java 笔记正文。

九、输出倾向

请输出一张：

- 白底；
- 简约；
- 教材式；
- 结构清晰；
- 适合插入 Java 笔记；
- 文字克制、排版规整；
- 视觉上专业、整洁、易读的示意图。

## 常用的 Windows 组合键

**Windows 组合键**（快捷键）是指通过同时按下一个或多个按键，快速完成某种操作的方式。熟练使用常见快捷键，可以显著提高日常学习、办公和文件管理的效率，减少频繁使用鼠标带来的操作成本。

### 系统与桌面操作类快捷键

- **`Win + D`**：**显示桌面**。按下后可以立即将当前所有窗口最小化，快速返回桌面；再次按下时，通常还能恢复原来的窗口状态。
- **`Win + L`**：**锁屏**。按下后系统会立刻进入锁定状态，需要重新输入密码或验证身份才能进入桌面。
- **`Win + I`**：**打开设置**。它可以直接进入 Windows 的系统设置界面，用于管理网络、蓝牙、个性化、账户、更新等功能。
- **`Win + R`**：**打开运行窗口**。运行窗口是 Windows 中非常高效的命令入口，可以通过输入程序名、系统工具名或路径来直接打开目标内容，例如输入 `cmd` 打开命令提示符，输入 `regedit` 打开注册表编辑器。
- **`Alt + F4`**：**关闭当前窗口**。当某个应用程序窗口处于激活状态时，按下该组合键可以直接关闭它；如果当前位于桌面，通常会弹出关机选项。

### 文件与资源管理类快捷键

- **`Win + E`**：**打开文件资源管理器**。它是文件管理中最常用的快捷键之一，可以快速进入磁盘、文件夹和文件的浏览界面，适合高频率进行查找、整理、复制和移动文件的场景。
- **`F2`**：**重命名选中的文件或文件夹**。在资源管理器中选中某个对象后按下 `F2`，可以立即进入重命名状态。相比右键菜单操作，这种方式更直接、更高效。
- **`Delete`**：**删除选中的文件或内容**。在文件管理中，按下后一般会将文件移入回收站，而不是立即彻底删除，因此通常仍有恢复的机会。这是日常最常用的删除方式。
- **`Shift + Delete`**：**永久删除选中的文件或内容**。这种删除方式通常会跳过回收站，直接执行更彻底的删除，因此风险更高。只有在确认文件不再需要时，才建议使用该快捷键。

### 编辑操作类快捷键

- **`Ctrl + C`**：**复制**当前选中的内容。
- **`Ctrl + V`**：**粘贴**剪贴板中的内容。
- **`Ctrl + X`**：**剪切**当前选中的内容。与复制不同，剪切的目的是“移动”内容，而不是“保留原处再复制一份”。因此它通常用于将文件、文字或对象从一个位置转移到另一个位置。
- **`Ctrl + A`**：**全选**当前区域中的所有内容。
- **`Ctrl + Z`**：**撤销**上一步操作。这个快捷键的意义在于快速纠正错误，例如误删文字、误移动文件、误修改内容等。它是日常编辑操作中极其重要的“补救”手段。
- **`Ctrl + Y`**：**恢复**刚刚被撤销的操作。它通常与 `Ctrl + Z` 成对出现：`Ctrl + Z` 是回退，`Ctrl + Y` 是前进。
- **`Ctrl + S`**：**保存**当前文件。
- **`Home`**：将**光标快速移动到当前行的行首**。当一行内容较长时，如果仅靠鼠标或方向键逐步移动，效率会很低，而 `Home` 可以让光标一步回到本行最前面，适合快速修改行首内容或准备在行首插入文本。
- **`End`**：将**光标快速移动到当前行的行尾**。它与 `Home` 相对应，主要用于快速跳转到本行最后，适合在一行代码、文本或命令的末尾继续补充内容。
- **`Shift + Home`**：从**当前光标位置开始，向左选中直到当前行行首**。它适合在不使用鼠标的情况下，快速选中当前光标前面的整段行内内容。
- **`Shift + End`**：从**当前光标位置开始，向右选中直到当前行行尾**。它与 `Shift + Home` 相对应，适合快速选中当前光标后面的整段行内内容。
- **`Ctrl + Home`**：将**光标快速移动到整篇文本的开头**。这里的“开头”不是当前行，而是当前文档、代码文件或文本区域的最开始位置。对于内容较长的文件，这个快捷键非常实用，因为它省去了大量滚动和手动定位操作。
- **`Ctrl + End`**：将**光标快速移动到整篇文本的末尾**。它与 `Ctrl + Home` 相对应，用于快速跳转到文档最后，适合在文章末尾追加内容、检查结尾部分或快速定位到最新编辑位置。
- **鼠标双击**：通常用于**快速选中一个单词**。这种方式在阅读文章、修改变量名、复制某个术语时非常方便，因为系统会自动以“单词”为单位进行选中，而不需要用户自己拖动精确范围。

### 窗口切换与任务管理类快捷键

- **`Alt + Tab`**：**切换应用程序窗口**。按住 `Alt` 再连续按 `Tab`，可以在当前打开的程序之间切换。它更适合快速跳转到另一个正在使用的窗口，是多任务场景下最常见的快捷键之一。
- **`Win + Tab`**：**打开任务视图**。与 `Alt + Tab` 不同，它不仅能显示所有打开的窗口，还能管理虚拟桌面，因此更偏向“全局查看和整理任务”，而不只是简单切换窗口。
- **`Win + 左方向键 / 右方向键`**：**窗口分屏贴靠**。该快捷键可以让当前窗口快速贴靠到屏幕左侧或右侧，方便与另一个窗口并排显示。它非常适合一边查资料、一边写文档的学习和办公场景。

## DOS 命令入门

### 什么是 DOS 命令

**DOS** 是 **Disk Operating System** 的缩写，中文通常译为 **磁盘操作系统**。早期个人计算机广泛使用 DOS 作为操作系统，后来随着 Windows 图形界面系统的发展，DOS 逐渐退出主流桌面操作系统舞台。

不过，在今天的学习和开发环境中，人们仍然经常把 Windows 中的命令行窗口习惯性地称为 **“DOS 窗口”**，把在其中输入的命令称为 **“DOS 命令”**。这种叫法在日常交流中非常常见，但从严格意义上说，现代 Windows 中使用的并不是真正的 DOS 系统，而是 **命令提示符**（Command Prompt）或其他命令行工具。

对于普通用户来说，很多操作可以通过鼠标和图形界面完成；但对于程序员，尤其是 **Java 软件工程师**，掌握基本命令行操作是必要的。

### 如何打开 DOS 命令窗口

在 Windows 中，最常见的打开方式是通过 **运行窗口** 启动命令提示符。具体操作为：先按下 **`Win + R`** 打开运行窗口，然后在输入框中输入 **`cmd`**，最后按下 **回车键**，即可打开 DOS 命令窗口。这里大家平时所说的“DOS 命令窗口”，实际上通常指的是 Windows 的 **命令提示符**（Command Prompt）。

打开命令窗口后，通常会看到类似下面这样的内容：

```
C:\Users\Administrator>
```

这表示命令行当前正处于某个具体位置，后面输入的命令就会以这个位置为基础执行。

> **补充**：`cmd` 是 Windows 中 **命令提示符程序** 的启动命令，因此在运行窗口中输入 `cmd`，本质上就是在启动命令提示符。

### 如何在 DOS 命令窗口中复制和粘贴

在 Windows 10 及之后的系统中，DOS 命令窗口已经支持比较方便的复制和粘贴操作。最常见的做法是：先用鼠标左键选中窗口中的目标内容，然后点击鼠标右键，此时选中的内容通常就会被复制到剪贴板中；如果要粘贴，只需要在目标位置再次点击鼠标右键即可。

> **注意**：这里所说的复制和粘贴，是指 **在 DOS 命令窗口内部** 进行的操作。

### 当前所在目录

在 DOS 命令窗口中，**当前所在目录** 指的是命令行此刻所处的位置，也可以叫做 **当前路径**。当窗口中显示类似 `C:\Users\Administrator>` 这样的提示信息时，`C:\Users\Administrator` 对应的路径就是当前所在目录。如果你此时执行文件相关命令，而命令中又没有特别写明其他路径，那么系统通常就会默认你要操作的是当前目录中的文件和文件夹。

## 常用的 DOS 命令

掌握常用的 DOS 命令，是后续进行 Java 开发环境配置、文件管理、程序编译与运行的基础。很多看起来“图形界面也能完成”的操作，在命令行中往往只需要一条命令即可完成，而且更快、更直接、更适合程序员的工作方式。下面整理的是最常见、最实用的一组基础命令。

### `dir`

`dir` 命令用于**列出当前目录下所有的文件和子目录**。当你进入某个目录后，如果想先看看这个位置下面到底有哪些内容，最常用的就是 `dir`。执行后，命令窗口会显示当前路径下的文件夹、文件、修改时间等信息。它的核心作用是“查看当前目录的内容”，因此在进行删除、进入目录、拷贝文件等操作之前，通常都会先执行一次 `dir`，确认当前位置和目录内容是否正确。

> **注意**：`dir` 默认查看的是**当前目录**中的内容，而不是整个磁盘中的所有内容。

### `cls`

`cls` 命令用于**清屏**，也就是清除当前命令窗口中已经显示出来的内容。它不会删除文件，也不会撤销之前执行过的命令，只是把屏幕显示区域清空，让界面重新变得整洁。这个命令在连续执行了很多命令、屏幕内容过多、不方便继续观察输出结果时非常有用。

> **注意**：`cls` 只是清除显示内容，并不会清除命令历史记录。如果想再次查看之前输入过的命令，仍然可以尝试使用方向键查找历史命令。

### `exit`

`exit` 命令用于**退出 DOS 命令窗口**。执行后，当前命令提示符窗口会直接关闭。它适合在命令行操作完成之后，用于结束当前会话。

### 切换盘符

在 DOS 命令窗口中，如果要从一个盘切换到另一个盘，可以**直接输入盘符加冒号**。例如当前在 `C:` 盘下，如果要切换到 `D:` 盘，只需要输入 `D:` 并回车即可。这种切换方式非常直接，不需要使用 `cd` 命令。

> **易错点**：很多初学者会写成 `cd D:`，这是不规范的。盘符切换应直接写 `D:`。

### `del`

`del` 命令用于**删除一个或多个文件**。它针对的是**文件**，而不是目录。如果要删除某个具体文件，可以直接写文件名；如果要批量删除一类文件，可以结合通配符使用，例如 `*.java` 表示删除当前目录下所有扩展名为 `.java` 的文件。

```
del A.txt
del *.java
```

> **注意**：`del` 默认删除的是**当前目录**中的文件，如果命令中没有写完整路径，就会以当前路径为准。

> **易错点**：`del` 不能直接用来删除目录；删除目录通常使用 `rd`。

### `ipconfig`

`ipconfig` 命令用于**查看本机的 IP 地址信息**。在网络通信中，IP 地址可以理解为计算机在网络中的地址标识，因此如果要进行网络调试、排查连接问题，通常会先查看本机 IP 配置。执行 `ipconfig` 可以看到比较基础的网络信息；如果执行 `ipconfig /all`，则可以看到更详细的内容，例如网卡信息、物理地址、DHCP 状态等。

### `ping`

`ping` 命令用于**测试当前计算机与目标主机之间是否能够正常通信**。目标可以是一个 IP 地址，也可以是一个合法域名。执行后，系统会向目标发送测试数据包，并根据返回结果显示是否连通、耗时是多少、是否存在丢包等信息。如果能够正常收到回复，通常说明网络连接是通的。

```
ping 192.168.1.100
ping www.baidu.com
ping www.baidu.com -t
```

`ping` 后面加上 `-t` 参数，表示**持续不断地发送测试数据包**，适合用来观察网络是否稳定。如果想终止当前正在执行的 `ping -t` 命令，可以按 **`Ctrl + C`**。

### `mkdir` 或 `md`

`mkdir` 命令用于**新建目录**，`md` 是它的简写形式，两者作用完全相同。这个命令的核心作用是在当前目录下创建一个新的文件夹，或者按照给定路径创建新的目录结构。在命令行中组织项目目录、练习文件夹、分类资料时，经常会用到它。

```
mkdir test
md demo
```

> **注意**：如果目录名中包含空格，实际使用时通常需要结合引号处理。

### `cd`

`cd` 是 **change directory** 的缩写，用于**切换目录**。它是命令行中最核心、最常用的命令之一，因为很多命令都依赖“当前目录”来执行。使用 `cd` 时，可以进入某个子目录，也可以返回上一级目录，还可以回到根目录。

```
cd 目录名
cd ..
cd /
```

其中，`cd ..` 表示**回到上一级目录**，`cd /` 表示**回到当前盘符的根目录**。`cd` 后面跟的路径既可以是**绝对路径**，也可以是**相对路径**。所谓绝对路径，就是从盘符开始写出的完整路径，例如 `E:\test`；所谓相对路径，就是相对于当前目录而言的路径，不从盘符开始写。绝对路径的优点是定位明确，不依赖当前位置；相对路径的优点是书写简洁，但前提是你必须清楚自己当前在哪个目录下。实际使用中，如果目录名字较长，还可以在输入一部分目录名后按 **`Tab` 键**，让系统自动补全目录名称，这样可以提高效率并减少输入错误。

```
cd E:\test
cd demo
```

### `rd`

`rd` 命令用于**删除目录**。如果目录是空的，可以直接删除；如果目录中还有内容，则需要加参数强制删除。对于不为空的目录，常见写法是 `rd /s /q 目录名`，其中 `/s` 表示连同目录中的内容一起删除，`/q` 表示安静模式，不再逐个确认。

```
rd test
rd /s /q test
```

> **注意**：`rd /s /q` 会删除整个目录及其内部所有内容，操作风险较高，必须谨慎使用。

### `ren`

`ren` 命令用于**重命名文件或目录**。它的作用与图形界面中的“重命名”一致，只不过是在命令行中完成。使用时，一般需要写出旧名称和新名称。

```
ren old.txt new.txt
```

### `copy`

`copy` 命令用于**复制文件**。它可以将一个文件复制到另一个位置，也可以复制后重新命名。这个命令在做文件备份、整理配置文件、保留代码副本时很常用。

```
copy A.txt B.txt
copy A.txt D:\backup\
```

> **注意**：`copy` 主要用于复制**文件**，不是目录。

### `shutdown`

`shutdown` 命令用于**执行关机操作**。通过它可以指定延迟多少秒后自动关机，因此常用于定时关机、脚本控制关机等场景。例如下面的命令表示在 **3600 秒** 之后关机，也就是 **1 小时后关机**。

```
shutdown -s -t 3600
```

其中，`-s` 表示关机，`-t` 表示设置延迟时间，单位是秒。

> **补充**：如果设置了延迟关机后又想取消，常见做法是使用 `shutdown -a` 终止本次关机计划。

> **练习**：
>
> 【单选题】在 DOS 命令窗口中，当前路径为 `C:\Users\Administrator`。如果此时想要**查看当前目录下所有文件和目录**，应该使用下列哪一条命令？
>
> - A. `cls`
> - B. `dir`
> - C. `cd ..`
> - D. `exit`
>
> 【单选题】关于 `cd` 命令、路径以及目录切换，下列说法中**错误**的是哪一项？
>
> - A. `cd ..` 表示返回上一级目录。
> - B. `cd /` 表示回到当前盘符的根目录。
> - C. `cd` 可以用来直接切换盘符，例如 `cd D:`。
> - D. 以盘符开头的路径通常是绝对路径。
>
> 【多选题】下列关于常用 DOS 命令的说法中，**正确**的有哪些？
>
> - A. `del *.java` 可以删除当前目录下所有扩展名为 `.java` 的文件。
> - B. `rd` 用于删除目录，`del` 主要用于删除文件。
> - C. `ping www.baidu.com -t` 可以持续检测网络连通性，按 `Ctrl + C` 可以终止。
> - D. `ipconfig /all` 可以查看比 `ipconfig` 更详细的网络信息。
> - E. `shutdown -s -t 3600` 表示立即关闭当前 DOS 窗口。
>
> 【答案】1. B；2. C；3. A、B、C、D。

## 批处理文件

**批处理文件** 是一种可以**按顺序批量执行多条 DOS 命令**的文件。它的常见扩展名是 **`.bat`**，因此也常被称为 **BAT 文件**。本质上，它并不是一种复杂的程序语言文件，而是把原本需要在命令窗口中一条一条手动输入的命令，提前写入到一个文本文件中，然后由系统一次性顺序执行。

也就是说，如果某些命令你经常要重复执行，例如切换盘符、进入某个目录、删除若干文件、复制文件、创建目录等，那么就没有必要每次都手工输入。此时可以把这些命令统一写进一个 **`.bat`** 文件中，之后只需要**双击这个文件**，系统就会自动按顺序执行里面的所有命令。这就是批处理文件最大的意义：**把重复性的命令操作自动化**。

从使用角度看，批处理文件的作用可以理解为：把“多步命令行操作”封装成“一次执行”。这样做的好处有两个：一是可以提高效率，避免重复输入；二是可以减少人为输入错误，尤其是在路径较长、命令较多的情况下，这种优势会更明显。

> **注意**：批处理文件本质上仍然是一个**文本文件**，只是它的扩展名不是 `.txt`，而是 `.bat`。正因为批处理文件本质上是文本，所以可以使用**文本编辑器**打开和编写，例如记事本等。

批处理文件中的命令通常是**自上而下、按顺序依次执行**的。也就是说，写在前面的命令会先执行，写在后面的命令会后执行。因此，在编写批处理文件时，命令的排列顺序非常重要，不能随意打乱。

例如，下面这组命令：

```
d:
cd test/xyz
del a.txt
del b.txt
```

它的执行逻辑不是“同时执行”，而是按照下面的顺序一步一步进行：

1. 先执行 `d:`，把当前盘符切换到 **D 盘**；
2. 再执行 `cd test/xyz`，进入 `D:` 盘下的 `test/xyz` 目录；
3. 然后执行 `del a.txt`，删除当前目录中的 `a.txt`；
4. 最后执行 `del b.txt`，删除当前目录中的 `b.txt`。

之所以必须按这个顺序写，是因为后面的删除命令依赖前面的路径切换结果。如果不先切换到正确的位置，那么 `del a.txt` 和 `del b.txt` 删除的就未必是目标目录中的文件，甚至可能找不到文件。

批处理文件可以使用普通的文本编辑器来编写。编写时，本质上就是把要执行的 DOS 命令一行一行写进去，然后将文件保存为 **`.bat`** 扩展名。保存之后，直接**双击 `my.bat` 文件**，系统就会自动打开命令环境，并依次执行其中的命令。